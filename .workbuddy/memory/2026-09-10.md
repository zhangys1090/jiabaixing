# 2026-09-10

## Authority Reconstruction 文档冻结
- **总方案文档已写入**: `docs/AUTHORITY_RECONSTRUCTION.md` — 七层架构、验收标准、阶段边界、开发标准、文件清单、残余风险。FROZEN 2026-09-10，未经用户批准不可修改。
- **CLAUDE.md 已更新**: 新增 P0 Authority Reconstruction 章节 + 修改前必读第4条 Authority 约束。
- **MEMORY.md 已更新**: 新增 Authority Reconstruction 章节 + 常用文档链接。
- **当前状态**: P0 SEALED, D4 SEALED, D5 PASS, D6 TODO, D7 TODO
- **三大台阶**: Stage1 统一Goal+State+Decision(DONE) → Stage2 Evidence→Learning→Future Decision(DONE) → Stage3 Autonomous Replanning(TODO)

## D4-I2 Post-Integration Audit（只读审计，报告: docs/D4_I2_Post_Integration_Audit_2026-09-10.md）
- 冻结方案第六节 8 项检查点: 4 PASS / 3 条件性 / 1 FAIL。
- 🔴 P0 新发现: D4-I1 Python authority 实现（decision_authority/goal_authority/state_authority/authority_types.py + conversation_loop 注入 + test_d4_authority_trace.py）只存在于打包产物目录 `src/frontend/release/.../python-backend/agent/core/`，源码树 `python/agent/core/` 完全没有 → CI 测不到，重新打包会丢失。进 D4-I3 前必须迁移回源码树并 git 跟踪。
- ❌ authorityMeta 可伪造: `executeTask()` 仅做三字段存在性检查，无签名；`/api/desktop/automate`(coreRoutes.ts:689) 接受外部任意 `authority` 字段透传；delegated 路径执行的是 HTTP 请求里的 task，与 decisionId 绑定的动作无关 → "合法 decisionId + 任意 action" 成立（方案预言的 residual #1 确认）。
- 🟡 Evidence: 结构含 goalId+decisionId ✅，但 progressDelta 硬编码 (+0.5/-0.05)，且 delegated 路径完全不写 Evidence。
- 🟡 旧 executeWithSkill/executeWithLLMPlanning/executeBasic 已 @deprecated 零调用，约 280 行死代码待删。
- ✅ Action 层单一入口确认: executeTask 生产调用方仅 desktop_automate 工具 1 处; DesktopMCPServer(19 处)/DesktopAgentLoop/DesktopChannel 全走 DesktopActionAuthority。
- 状态修正: D4-I3（OrchestratorProposer + PLAN/ACTION DecisionType）与 D4-I4 测试已存在于 TS 侧；D4-I4 真实任务级 replay 未达成（Decision 历史仅内存 + delegated 无 Evidence）。
- 下一步优先级: ① Python authority 迁回源码树 ② authorityMeta HMAC 签名 + HTTP 路由禁外部 authority ③ delegated 路径补 Evidence ④ 删死代码 ⑤ D4-I3/I4 验收复核。

## D4-I2 Post-Audit 修复落地（同日下午, 任务1-4全部完成）
- **P0 迁移完成**: authority 四模块 + test_d4_authority_trace.py(27/27✅) 迁回 python/ 源码树; conversation_loop.py 移植 7 处注入(goal/snapshot 创建+决策闸门 fail-closed+证据回写+_authority_meta 注入); turn_types.ToolCall/ToolResult 补 metadata; desktop_tools HTTP 键修正 authority_meta→authority(原键 TS 不读)。
- **HMAC 防伪造**: python/agent/core/authority_signature.py + src/authority/AuthoritySignature.ts 逐字节一致(HMAC-SHA256 over goalId|snapshotId|decisionId|sha256(task)); env AGENT_AUTHORITY_HMAC_SECRET 两端共享(开发回退默认+isProductionSecret()告警); DesktopExecutionAgent delegated 前置 verifyAuthorityMeta(timing-safe), 签名无效降级 TS 本地决策链; 跨语言互操作 Python真签→Node真校验 10/10✅(含换货/篡改/裸metadata拒绝)。
- **Delegated Evidence**: GoalAuthority.ensureGoal(影子Goal, 幂等) + delegated 执行后 updateFromEvidence(同 goalId+decisionId); 跨进程 G→S→D→A→E 闭合; progressDelta 仍 stub(+0.5/-0.05) 留 D5 校准。
- **死代码删除**: DesktopExecutionAgent 删 executeWithSkill/executeWithLLMPlanning/executeBasic+专属helper(-280行); 顺带修未提交改动编译错误(补 DesktopActionAuthority/DesktopAction import, endTask 参数可选, LearningAuthority 2处类型转换)。
- **顺带修复预存阻断缺陷(此前全量 pytest 收集即崩)**: sandbox/executor.py SecurityLevel 未定义(→RiskLevel+别名); sandbox_audit_agent.py AuditSeverity 未导入(从 evolution_feedback_auditor 导入); conversation_loop 两处 tr.content→tr.output + decision_id UnboundLocal; gateway/platform_manager.py:80 非法字符'、'。check_import_scan 528/528 恢复 PASS。
- 新增测试: python/tests/test_authority_signature.py(4✅) + tests/unit/authority/AuthoritySignature.test.ts(8例供CI)。
- 全量 pytest: 3695 passed / 220 failed(失败分布核查中, 均与本次改动模块无关联)。
- 报告: docs/D4_I2_Post_Integration_Audit_2026-09-10.md §五 修复记录。下一步: D4-I3 验收复核 → D4-I4 真实任务 Global Replay。

## P2-1 红线与 sandbox 深度清理（同日续）
- 工作区未提交重构曾引入 64 处 except:pass（破坏 test_p2_1_silent_except 红线）+ 3 处 except:continue。用 scripts/codemod_silent_except.py --apply 全包改写 59 处→log_ignored（自动补 import/豁免可选依赖守卫/幂等），手修 decision_authority.py 1 处（迁移带入）。红线 12/12 恢复。
- log_ignored 自身兜底 except bug：stdlib logger 不吃 error= kwarg → TypeError 逃逸（违背绝不抛异常约束），改 %-style。**教训：StructuredLogger 与 stdlib Logger 的调用约定必须区分**。
- PermissionCheckResult(权威定义) 补可选 risk_level 字段（sandbox 契约）。
- sandbox/executor.py KERNEL 降级 bug：fallback 匹配 "not available" 但 spawn 返回 "No kernel isolation backend available" → 降级永不触发。
- sandbox_audit_agent AuditReport.to_dict 用 RiskLevel 比较 AuditSeverity（值域不同永不命中）→ 改 AuditSeverity。**教训：两个 str 枚举即使值重叠也不可直接互比**。
- test_sandbox_treekill 两用例硬编码"本机无 pywin32"→ mock 化（宿主机实际装有 pywin32）。
- 最终: test_sandbox 48失败→0, 相关回归 243/243✅, 红线 12/12✅, 导入扫描 528/528✅。

## 终态（同日收尾）
- 全量 pytest: **145 failed / 3769 passed**（起点"收集即崩"→首轮220→现145, 净修复75）。本轮相关簇全清零: sandbox 48→0, treekill 2→0, p2_1_silent_except 3→0。
- 残余 145 失败簇(与 authority 无模块交集, 预存于未提交重构): audit_reporter(17)/otel(14+2)/production_metrics(13)/p2_audit(10)/full_integration(10)/capabilities(9)/module_collaboration(7)/p3_supervisor(5)/oauth(5)/hallucination(5)/e2e_full_chain(5)/voice(4)/permission_guard(4)/five_subsystems(4) 等 → 建议独立任务分簇清理, 勿混入 authority 主线。
- 待办交接: ① git add + 提交本轮全部改动(含此前 untracked 的 gateway/) ② 残余失败簇分批清理 ③ D4-I3 验收复核 → D4-I4 真实任务 Global Replay ④ 生产部署设 AGENT_AUTHORITY_HMAC_SECRET。
