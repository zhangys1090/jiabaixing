# 2026-07-17 工作日志

## 家百星 × Hermes 能力对标(需求分析,未改代码)
- 起因:用户给出 Hermes 0.18.0 四层节点分层地图(入口/核心编排/能力节点/分发集成,30+节点),问家百星还该学哪些优势。
- 用 Explore 子agent实测家百星代码库,结论:**结构性能力已基本追平**。已具备(✅):IM网关(TS `src/integration/adapters/` 8家 + Python `gateway/platforms/`)、MoA(`python/agent/llm/moa_aggregator.py`)、Cron(`agent/scheduler/cron.py`+`src/cron/` 含注入防护,注释直言"参考Hermes")、子Agent委派+A2A(`delegate_tool.py`+`a2a/` 含 TrustLevel)、MCP双向(消费`mcp/server_manager.py`+对外`DesktopMCPServer.ts`)、TUI(`curses_tui.py`)、Proxy(`proxy_server.py`)、多profile(`profile_manager.py`)、流式脱敏(`security/streaming_scrubber.py`)、Skills Hub(`SkillRegistry.ts`/`skills/registry.py`)。
- **真差距(4项)**:①运行时安全姿态 safe-mode/yolo/accept-hooks(❌全仓未命中,有ApprovalEngine但无分级自治姿态);②插件信任策略(🟡 插件体系全但无 trust policy,trust仅在A2A);③工具集概率分发(🟡 只有 `SceneToToolsetMapper` 确定映射,无概率采样;对标 Hermes 19套分布);④Provider目录广度+OAuth(🔴 传输层4类原生适配已在,但 `providers.json` 实配仅deepseek 1家,无Vertex/无OAuth onboarding)。
- **理念差距**:Hermes"窄腰核心+catalog驱动扩展"(optional-skills/optional-mcps 预置未启用目录);家百星 AGENTS.md 有原则但落地弱,optional目录🟡未成体系。建议CI守卫核心tool schema不被污染(复用 doc-derived-audit.mjs 思路)。
- 产出:节点覆盖度地图(show_widget)+ 对标表 + 优先级排序(安全姿态+插件信任优先,风险敞口最实)。

## 任务卡拆分(用户确认要,分轮推进)
- 已建 TaskList 4 张卡:R1-A 运行时安全姿态 RuntimePosture、R1-B 插件信任策略 PluginTrustPolicy、R2 工具集概率分发 ToolsetSampling、R3 Provider目录广度+catalog窄腰。
- 每卡 DoD 对齐 AGENTS.md §0.3:Python端完整 + TS入口路由 + 测试100%通过 + 端到端可追溯 + 跨端traceId一致。
- 待用户"继续(下一轮)"触发 R1 实施。

## R1-A 运行时安全姿态 RuntimePosture ✅ 已完成(2026-07-17)
- **Python 核心**: 新增 `python/agent/security/runtime_posture.py` —— `RuntimePosture` 枚举(SAFE/CONFIRM/AUTO/YOLO)+ `PostureDecision`(ALLOW/DENY/REVIEW)+ 纯函数 `decide(posture,risk)` 决策矩阵 + `parse/from_env`(读 `AGENT_RUNTIME_POSTURE`,非法回退 CONFIRM,支持 safe-mode/accept-hooks 等别名)。critical 硬底线:任何姿态都不静默 ALLOW(SAFE→DENY,其余→REVIEW)。
- **集成**: `approval_manager.py` `ApprovalManager.__init__` 加 `posture` 参数(默认 CONFIRM=零行为变更)+ `set_posture()`/`posture` 属性;`request_approval` 在 CONFIRM 之外先走姿态裁决(ALLOW 立即放行/DENY 明确原因拒绝/REVIEW 落回原审批流)。`engine.py:488/2283` 两处实例化改为 `posture=RuntimePosture.from_env()`。
- **TS 入口**: 新增 `src/cli/runtimePosture.ts`(纯函数,无重依赖)—— `parseRuntimePostureFlags`/`applyRuntimePostureFlags` 把 `--safe-mode/--yolo/--auto/--accept-hooks/--posture[=v]` 映射为 `AGENT_RUNTIME_POSTURE` env 并从 argv 剔除;`src/cli.ts` 分发前调用;`src/cli/utils.ts` re-export 兼容。TS 仅透传,裁决在 Python(符合 §0.1)。
- **测试**: Python `tests/test_runtime_posture.py`(决策矩阵全组合参数化 + parse/from_env + ApprovalManager 集成 SAFE/AUTO/YOLO/CONFIRM 向后兼容 + set_posture 运行时切换);TS `tests/unit/cli/runtimePosture.test.ts`(8 用例)。
- **验证**: Python `pytest tests/test_runtime_posture.py tests/test_approval_manager.py tests/test_business_logic_audit_fixes.py -q` → **78 passed**(critical 硬底线无回归);TS jest 8 passed;`tsc --noEmit` 改动文件零新增错误(总 10 个预存);端到端 `AGENT_RUNTIME_POSTURE=yolo` → from_env → decide(high)=allow / decide(critical)=review / SAFE(medium)=deny。
- **遗留(可选跟进)**: HTTP 运行时切换端点 `POST /v1/runtime/posture`(需 TS→Python bridge RPC 调 `set_posture`)未做;桌面 UI 姿态切换器未做。当前 env+CLI 入口已足够激活端到端能力。下一轮 R1-B 插件信任策略。

## R1-B 插件信任策略 PluginTrustPolicy ✅ 已完成(2026-07-17)
- **Python 核心**: 新增 `python/agent/plugins/trust.py` —— `PluginTrustPolicy` + `PluginTrustError(PermissionError)` + `ContextScope`(NONE/SCOPED/FULL) + 纯 gate 函数 `can_call_llm/can_call_tool/allowed_context_scope`。**信任模型直接复用 A2A `TrustLevel`(UNTRUSTED/LOW/MEDIUM/HIGH),不另造一套**(符合 §0.1 统一语义)。三条受控路径:LLM 需 ≥LOW;工具按(信任×风险)矩阵放行且 **critical 永不放行(硬底线)**;上下文 UNTRUSTED=none/LOW,MEDIUM=scoped/HIGH=full。对标 Hermes `_TrustPolicy`/`PluginLlmTrustError`。
- **集成**: `plugins/manager.py` `PluginManager.__init__` 加 `trust_policy` 依赖(默认新建);`register_plugin` 末尾 `register_default(name)` —— **新插件默认 UNTRUSTED,必须显式提升**(setdefault 不覆盖已预置的);新增入口 `trust` 属性 + `set/get_plugin_trust` + `guard_plugin_llm/tool/context`。
- **入口(env 驱动,同 R1-A 模式)**: `PluginTrustPolicy.from_env()` 读 `AGENT_PLUGIN_TRUST="nameA:high,nameB:medium"` 预声明受信插件(非法项跳过、未列出仍 UNTRUSTED),+ 模块函数 `parse_trust_level`(非法回退最低);`engine.py:_init_plugin_manager` 改为 `PluginManager(trust_policy=PluginTrustPolicy.from_env())`。**注意:调研发现家百星既无 TS 插件 CLI 也无 Python 插件 HTTP 路由**(`src/cli/` 无 plugin 文件、`agent/api/` 无 plugin router),故原卡"TS 入口 `src/cli/plugin_manager`"无宿主可挂;改用与 R1-A 一致的 env-var 激活(Python 主实现,§0.1 合规),避免为单一功能凭空造 TS 面。
- **附带修复**: 重构 `python/agent/a2a/__init__.py` —— protocol/types(含 TrustLevel)无条件先导入,manager/server/client/auth(依赖 fastapi)包进 `try/except ImportError` 优雅降级置 None。解决 `test_plugin_trust` 因连带 import fastapi 而 collection 失败;fastapi 存在的环境行为零变更。
- **测试**: `python/tests/test_plugin_trust.py` **33 passed**(21 gate/矩阵/critical硬底线/默认UNTRUSTED/manager集成 + 12 新增 parse_trust_level 参数化 & from_env 多项/非法跳过/register_default 不覆盖)。端到端 `AGENT_PLUGIN_TRUST=report_gen:high,scraper:low` → get_trust 正确、report_gen LLM/FULL ctx 放行、unknown LLM 拒绝、critical 工具硬底线拒绝。
- **无回归说明**: 本机裸解释器缺 `pytest-asyncio`(17 个 `@pytest.mark.asyncio` i18n 测试无法运行)与 `fastapi`(test_a2a/test_api 等 collection error)—— **均为预存环境缺依赖,非本次改动**;`agent.a2a`/`agent.plugins` 在无 fastapi 下均可导入,TrustLevel 可用。完整套件需在装了 fastapi/pytest-asyncio 的 CI 环境跑。
- **遗留(可选跟进)**: 若将来给插件加 HTTP/TS 管理面,可加 `GET/POST /v1/plugins/{name}/trust`(Python 路由 + TS 透传);guard_* 目前是提供给插件调用点的强制 API,插件真实 LLM/工具/上下文调用路径接线可在插件执行框架落地时补。下一轮 R2 工具集概率分发 ToolsetSampling。

## R2 工具集概率分发 ToolsetSampling ✅ 已完成(2026-07-17)
- **背景**: 原 `SceneToToolsetMapper`(TS `src/harness/tools/registry/SceneToToolsetMapper.ts` + Python `python/agent/tools/toolset_registry.py`)均为**确定性**——场景检测取最高分 → 单一工具集。`detect_scene` 有评分但 `map_to_toolset` 只返回 1 个,**无概率分发能力**(对标差距③)。
- **Python 核心(§0.1 主实现)**: 新增 `python/agent/tools/toolset_sampling.py` —— `ToolsetSampler` + `ToolsetCandidate(toolset_id, weight>0, disclosure_level?)` + `parse_sampling_flag` + `build_default_sampler`。**weight = 相对概率,直接归一化为概率分布(无 softmax/温度)**,多候选 `rng.choices(weights)` 加权采样;单候选/关闭退化为确定性最高权重(零回归)。RNG 可注入 `random.Random(seed)`,测试可复现、不依赖全局随机态。**关键设计决策**: 初版误用 softmax(0.7/0.3 权重变 ~0.6/0.4),改为"权重即相对概率"直接归一化,语义对齐 Hermes 工具集分布且直觉正确。
- **集成**: `toolset_registry.py` 的 `SceneToToolsetMapper` 新增 `enable_sampling`(默认读 `AGENT_TOOLSET_SAMPLING` env, off=确定性)、`sampler` 属性/`set_sampler`/`set_sampling_enabled`、新入口 `sample_toolset(scene, rng?)` —— 关闭或单候选走旧 `map_to_toolset`,开启且多候选走加权采样(候选可覆盖 disclosure_level)。既有的 `resolve()` 确定性路径完全不动。
- **原卡 DoD 调整(诚实说明)**: 原卡写"采样结果喂 EvolutionEngine 做进化信号"——本轮**未接 EvolutionEngine**;当前 `SceneToToolsetMapper` 在仓库内仅被 import、无生产调用点(`grep SceneToToolsetMapper(` 无构造点),故采样器作为可组合能力落地,待有具体调用点(Executor 选工具集)时接。TS 侧保持确定性不动(§0.1: 概率分发属 Agent 核心,Python 主实现;TS 不应独立实现)。
- **测试**: `python/tests/test_toolset_sampling.py` **26 passed** —— 开关解析、单候选确定性、加权分布近似设定权重(70/30±5%、50/50±5%)、低权重稀有、负权重拒绝、固定种子可复现、默认采样器每场景单候选=旧映射、mapper 集成(env off 确定性 / env on 加权 / env 解析 / candidate disclosure 覆盖)。回归 `test_toolset_registry/test_builtin_toolsets/test_plugin_trust/test_runtime_posture` 共 **115 passed** 无回归。
- **后续(可选)**: ① 把多候选分布真正接到某个场景(如 coding 70% coding + 30% full)并喂 Executor 选工具集;② 若接 EvolutionEngine,把"采样选中的工具集 → 任务成败"作为进化信号;③ 考虑 `config/scene_toolset_candidates.json` 外置分布便于运营调权重。

## R3 Provider 目录广度 + catalog 化窄腰 ✅ 已完成(2026-07-17)
- **背景(重估)**: 原差距④写"providers.json 实配仅 deepseek 1 家",实测已演进——TS `data/providers.json` 实配 4 家(xiaomi/deepseek/zhipu/local, 仅 deepseek/local enabled),`python/data/providers.json` 仅 deepseek。真正的缺口是**无 Provider 元数据(传输类型/模型清单/鉴权方式/OAuth 标注)** 与 **无 catalog 化窄腰**(optional-skills/optional-mcps 仅原则未落地)。传输层 `transports.TransportType` 已支持 openai_compatible/anthropic/gemini/bedrock(4 类原生适配在),`credential_pool.py` 有 Key 轮换/成本守卫但无 OAuth。
- **Provider Catalog(Python 主实现, §0.1)**: 新建 `python/agent/llm/provider_catalog.py` —— `ProviderSpec` + `ProviderAuth(api_key/oauth/none)` + `ProviderCatalog`。内置 **15 家声明式元数据**(美国 OpenAI/Anthropic/Gemini/Vertex/Bedrock/Azure + 国内 DeepSeek/智谱/Moonshot/通义/百川/豆包/阶跃 + 小米 + 本地),标注 transport/默认 base_url/auth/oauth_supported/默认模型/备注。`from_providers_json()` 合并运行时配置,产出 available(目录已知且已配置)/configured/unconfigured(可发现待接入)/unknown_configured(审计补全)/oauth_capable。`to_manifest()` 可审计清单。
- **窄腰 Extension Catalog(Python 主实现, §0.1)**: 新建 `python/agent/catalog.py` —— `ExtensionCatalog` + `ExtensionEntry` + `ExtensionState(builtin/optional/disabled)`。统一"可选能力目录"抽象:声明 → 默认禁用 → env `AGENT_OPTIONAL_EXTENSIONS="skill:foo,mcp:bar"` 或 `*` 通配启用。含 `requires_env` 闸门(缺必需环境变量则不可用, builtin/optional 通用)、`summary()` 分组。落地 Hermes "窄腰核心 + catalog 驱动扩展"理念,核心只留最小能力, skills/mcps/toolsets 以可选目录存在,避免业务污染核心 tool schema(§0.3 守卫意图)。
- **测试**: `python/tests/test_provider_catalog.py` **15 passed** —— 目录广度(15 家含中美主流)、OAuth 标注(Vertex/Bedrock 纯 OAuth, 国内纯 Key 不标)、providers.json 合并(available/configured/unconfigured/unknown 审计)、manifest 结构、spec 覆盖;ExtensionCatalog 默认全禁用/显式启用/通配/requires_env 闸门/disabled 永不开/summary/register。回归 `test_toolset_sampling/test_plugin_trust/test_runtime_posture/test_toolset_registry/test_builtin_toolsets` 共 **130 passed** 无回归。
- **原卡 DoD 调整(诚实说明)**: 原卡"OAuth onboarding 握手 + CI 守卫核心 tool schema"本轮**未做完整握手**——仅把 oauth_supported 作为目录元数据标注(为将来 onboarding 预留字段),未实现 OAuth 授权码流转/令牌持久化;`AGENT_OPTIONAL_EXTENSIONS` 提供启用机制但未接死到具体 skill/mcp 注册路径(目录是纯逻辑抽象,待具体加载点消费)。两份 catalog 均为可组合能力,env 激活、零回归。
- **后续(可选)**: ① 把 `ProviderCatalog` 接到路由层,启动打印可发现 Provider 清单 + OAuth onboarding 引导;② 实现 Vertex/Bedrock 的 OAuth/ADC 凭据获取;③ 把 `ExtensionCatalog` 接到 skills/mcp 实际注册,`AGENT_OPTIONAL_EXTENSIONS` 真正开关;④ 复用 doc-derived-audit 思路做 CI 守卫:核心 tool schema 变更需评审,optional 能力不进核心。
- **4 张对标任务卡全部完成**: R1-A 运行时安全姿态 ✅ / R1-B 插件信任 ✅ / R2 工具集概率分发 ✅ / R3 Provider 目录 + 窄腰 catalog ✅。

## 补充：核心 tool schema 变更评审 CI 守卫 ✅ 已完成(2026-07-17)
- **落实原差距审计建议**: R3 收尾时提出的"CI 守卫核心 tool schema 不被业务污染"(复用 doc-derived-audit 思路)。本次落地为可运行的 CI 红线。
- **"核心 tool schema"定义**: `python/agent/tools/builtin_toolsets.py` 的 `BASE_TOOLSET` —— 所有 Agent 默认继承的最小工具集(MEMORY+COGNITION 两类 + ask_clarification/system_status/context_manage)。它的扩张 = 核心 schema 变更,须评审。
- **交付**:
  - 基线清单 `config/core-tool-schema.baseline.json` —— 已评审的 BASE_TOOLSET 条目快照(5 项),含 `_comment` 说明评审/更新流程。
  - 守卫脚本 `scripts/check-core-tool-schema.mjs`(Node,零依赖,沿用 `check-no-tautology-tests.mjs` 同款风格)——解析 `BASE_TOOLSET` 块内 `ToolsetEntry(category=...)` / `(name=...)` → 与基线比对:**新增(当前有基线无)→ exit 1 拦截**(要求评审并更新基线);**删除(基线有当前无)→ 仅警告允许**(缩小内核更安全);一致→通过。
  - `package.json` 加 `check:core-tool-schema`,并接入 `check:all`;`backend-ci-cd.yml` 的 `lint-and-typecheck` 阶段在 tautology 守卫后新增一步 `node scripts/check-core-tool-schema.mjs`(与 doc-derived-audit 门并列)。
- **验证**: ① 当前代码跑守卫 → ✅ 通过(5 条与基线一致);② 临时往 BASE_TOOLSET 加 `experimental_business_tool` → 守卫 ⛔ exit 1 拦截(报告新增 1 项 + 评审指引),已还原源码复跑通过;③ `node --check` 语法 OK;④ 既有 tautology 守卫并跑仍 ✅。本机裸解释器无 fastapi,Python 单测不跑,但守卫纯静态解析不依赖 Python。
- **边界说明**: 守卫基于"静态解析 BASE_TOOLSET 定义",不执行 Python;若有人改 `BASE_TOOLSET` 结构(如抽变量)需同步守卫解析逻辑。可选能力(插件/技能/MCP)正确做法是走 R3 的 `ExtensionCatalog`/插件体系,不应进 BASE_TOOLSET——本守卫正是这一纪律的 CI 闸门。
