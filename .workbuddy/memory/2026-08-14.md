# 2026-08-14 工作日志

## 代码全面审计（错误吞没 / 信号 / 逻辑）
- 自研静态扫描器 `audit_scanner.py`（AST 解析 Python except + 正则扫 TS catch/signal），结果 `/tmp/audit_findings.json`。
- 产出报告 `code_audit_report_2026-08-14.html`（三类问题）。
- 关键核实结论：
  - 信号(高): `src/main.ts:439/444` uncaughtException/unhandledRejection 仅记日志不退出(僵尸进程)；`gatewayWorker.ts:145/154` 同；`forensics.py:227` SIGTERM/SIGINT handler 内调用 record() 非 async-signal-safe 且仅记录不关闭。
  - 吞错(高): `python/agent/api/` 共 104 处 `except Exception: return {error}` 服务端不记日志(ruff E722/S112 拦不住此形式)；`cognition.py:42`/`feedback.py:45,110` 误标 "invalid JSON body"。
  - 逻辑(高): `scheduler/cron.py` 慢任务每 tick 重复派发(next_run/status 异步才更新, guard 失效)；cron 表达式 next_run 退化为 +3600s(`_next_cron_run` 未用)；stop() 无事件循环分支泄漏 leader 锁/MQ。
  - TS 8 处空 `.catch(()=>{})` 吞失败(AuditService/OrchestratorAgent shutdown 等)。
- 审计盲区提示: ruff 门禁未覆盖 `except Exception: return` 模式，建议加自定义 AST 检查或统一 FastAPI 异常处理器。
