# 2026-07-28 工作日志

## KIMI K3 开源(2026-07-27) — jiabaixing 学习方向决策
- K3: 2.8T MoE(896专家/激活16), 原生视觉, 1M上下文; 开源 MoonEP/FlashKDA/AgentEnv。
- **该学(框架相关)**: ① AgentEnv 沙箱快照/恢复/分叉 → 补强 sandbox 模块(当前测试失败); ② 把 K3 接进 llm/provider_catalog.py(现有15家); ③ 长上下文策略优化上下文压缩 Pipeline; ④ Agent 评测维度。
- **不用学(训练侧Infra, 框架用不上)**: MoonEP(专家并行通信库)、FlashKDA(注意力GPU算子)、KDA/AttentionResiduals/GatedMLA 模型架构。

## 项目体积减肥排查(2.3G → 实测含隐藏约2.6G)
- 可安全删除(可重建, 用户确认本次不删, 仅记录): `.venv` 287M + `node_modules` 285M + `src/frontend/node_modules` 1.2G + `src/frontend/release` 442M(Electron打包) + `logs` 259M + `coverage` 70M + `dist` 20M ≈ 2.5G。
- 真正大头是 `src/frontend`(1.6G), 其中 node_modules 1.2G 占绝大比例。
- **禁删**: `data/`(10个SQLite db+记忆)、`src/frontend/src`(真源码)、`package-lock.json` x2、`.git/`。
- "坏虚拟" = .venv + 两个 node_modules, 损坏时删后重装最干净。
