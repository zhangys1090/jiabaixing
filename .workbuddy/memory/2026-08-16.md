# 2026-08-16

## 代码审计三类问题逐条修复（审计基线 2026-08-14）

按审计报告 `code_audit_report_2026-08-14.html` 三类问题逐条修复并验证。

### 信号阻塞/拦截（已修复）
- `src/main.ts:451` `unhandledRejection` 原仅记日志不退出 → 补 `process.exit(1)`（与 uncaughtException 一致，防僵尸）。
- `src/integration/gatewayWorker.ts:145/154` 两个处理器原仅上报不退出 → 补 `process.exit(1)`（try 包裹 send 容错）。
- `python/agent/gateway/forensics.py` 信号处理器原在 handler 内同步调用 `self.record()`（构造对象/写盘/psutil，非 async-signal-safe）且仅记录不转发关闭 → 改为**自管道（self-pipe）+ 守护线程**：handler 内仅 `os.write`（异步信号安全），守护线程读取后执行 record 与可选 `_shutdown_callback`。新增 `threading` 导入、`set_shutdown_callback()`。
- 复核：`shutdown.ts` 已有 `isShuttingDown` 重入守卫、`repl.ts` 二次 Ctrl+C 已实现退出、`main.ts uncaughtException` 已 `exit(1)` —— 这些审计项当前代码已正确，无需改动。

### 错误被吞（已修复）
- 8 处 TS 空 `.catch(()=>{})`（`DesktopVisionEngine`/`FeedbackLoops`×2/`OrchestratorAgent`/`DockerBackend`/`memory_recall`(补 Logger 导入)/`AuditService`/`WsProcessor`）→ 改为 `Logger.warn/debug` 记录失败。`memory_recall.ts` 补 `import { Logger }`。
- `cognition.py:42`、`feedback.py:45/110` 用 `except Exception` 固定返回 "invalid JSON body" 掩盖真实错误 → 收窄为 `except (json.JSONDecodeError, ValueError)`，其余异常如实抛出由中间件记录；两文件补 `import json`。
- `python/agent/main.py` 新增 `APIErrorLoggingMiddleware`（BaseHTTPMiddleware，最外层）：统一记录 4xx/5xx 及「200 + 错误体」响应，覆盖 api/ 下 100+ 端点服务端零日志盲区（无需逐文件改）。

### 逻辑错误（已修复）
- `cron.py` `_safe_dispatch` 派发前同步置 `job.status="running"` 并落盘（原 next_run/status 仅在 _run_job 完成后才更新、调度锁 publish 后即释放 → 慢任务每 tick 重复派发）。派发失败回滚 idle。新增回归测试 `test_safe_dispatch_marks_running_before_execution`。
- `cron.py` `stop()` 无运行循环分支原直接 `return` 泄漏 Redis leader 锁/MQ → 改为 `asyncio.run(...stop())` 尽力同步释放（try/except + log_ignored）。

### 验证
- Python: 5 文件 `py_compile` 通过；`ruff check` 全绿；`pytest python/tests/test_cron_leader.py` 6/6 通过（含新增回归）。
- TS: 8 处空 catch 已确认替换、新 Logger 调用就位；因本机 node_modules 损坏无法跑 tsc/jest（已知限制），改动为机械 `.catch(()=>{})`→`.catch((err)=>Logger.x())` 且与各文件既有 Logger 用法一致。

## 代码审计第二轮（2026-08-16 下午）
在首轮修复基础上，深入核心模块做第二轮全面复核 + 重点修复（报告 `code_audit_round2_2026-08-16.html`）。

### 本轮修复
- 🔴 R-01 `python/agent/loop/controller.py` `_parse_react_thought()`：旧正则 `Args:\s*(\{.+?\})` 非贪婪，嵌套 JSON 参数（如 `{"filters":{"type":"x"}}`）被首个 `}` 截断为非法 JSON → `tool_args={}` 整体丢失。改为平衡括号扫描（跟踪 `{}` 深度 + 字符串/转义）。新增回归测试 `python/tests/test_react_parser.py`（4 例，旧代码下必失败）。
- 🟡 R-02 `python/agent/memory/store.py` `close()`/`__del__()` 原 `except Exception: pass` 静默 → 改为 `log_ignored(log, ...)`（与 P0-3 静默异常基线一致）。

### 本轮核查排除（误报/设计性降级，非缺陷）
- `orchestration/fanout.py:221` asyncio.gather 无 return_exceptions → 安全：子任务 `_execute_with_semaphore` 已 except 返回 SubTaskResult 永不抛。
- WS `JSON.parse(message)`（index.ts:202 / desktopEventStream.ts:44）→ 安全：均在 try/catch 内且 catch 已 Logger 记录。
- controller.py 9 处能力探测 except 返回默认值 → 设计性安全降级，建议后续统一加 log_ignored（低优）。
- tool_call_guard / v2_engine._execute_plan / executor._retry_with_reflection / core/engine / context_references → 读源码确认逻辑正确。
- `dict.get(k,[])` ×1161 → 误报：Python 每次调用重新求值默认对象，非共享可变状态。

### 验证
- `ruff check` 全部改动（含首轮 6 文件）全绿；`pytest test_react_parser + test_cron_leader` 10/10 通过。
