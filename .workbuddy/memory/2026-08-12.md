# 2026-08-12 工作日志

## P0 安全与可靠性路线图收尾(全部完成)
- P0-1 ASR 命令注入: `src/multimodal/SpeechRecognizer.ts` 4 处 `execSync` 字符串拼接 → `execFileSync` + argv 数组(无 shell); 加 `validateWhisperInputs` 白名单(模型名 `^[A-Za-z0-9_.-]+$`、语言 `^[a-z]{2}(-[A-Za-z]{2})?$`、临时路径须落在 `process.cwd()/tmp` 且以 `.wav` 结尾)。`tests/unit/multimodal/SpeechRecognizer.security.test.ts` 6 例(奈何 jest 本机不可用, 已用 tsc 转译真实源码 + Module._load 桩做运行时验证: 正常模型走离散 argv、恶意模型被白名单 fail-closed、路径穿越被拒)。
- P0-2 failover: `agent/llm/router.py` 加 `_as_set`/`fallback_chain`(排除失败集合含刚失败 primary); `provider.py` 重写多 provider 退避重试(指数退避+jitter+`LLM_TIMEOUT` 显式 timeout)+ transport 路径异常降级 litellm。`tests/test_llm_failover.py` 4 例通过。
- P0-3 静默异常红线: 16 文件 33 处 `except:pass` → `log_ignored`(基线收 0); 另补修 11 处 `except:continue` 静默吞(relay_adapter/interaction_checkpoint/episodic_memory/skills/registry/browser_tools/code_tools×2/file_tools×2/test_gen_tools×2, 改 `except Exception as e: log_ignored/log.warning; continue`)。`python/pyproject.toml` 加 `[tool.ruff.lint] select=["E722","S112"]`; `backend-ci-cd.yml` python-test job 加 `ruff check agent/ --select E722,S112` 门禁。验证: ratchet=0、`test_p2_1_silent_except`=12/12、ruff agent/ 干净、P0 pytest 16 passed。
- 环境坑: `python/data/prompt_cache.db` 遗留只读文件曾致 failover 测试报 `sqlite3 readonly`; 删除后重建即绿(非逻辑回归)。本机 `node_modules` 损坏无法跑 jest。

## 下一步
- P0 已全绿, 待用户确认启动 P1(P1-1 TS A2A 薄壳以 python/agent/a2a/ 为基准)。

## P1-1 补齐 TS 端 A2A 薄壳(完成)
- 新建 `src/a2a/`: `types.ts`(复用 AgentRegistry 既有 TS 类型, camelCase 对齐 Python types.py)、`A2ARouter.ts`(`registerA2ARoutes(app)` 把 `/a2a/*` 透明转发到 `PYTHON_AGENT_URL`+originalUrl, 鉴权头/JSON 体透传, 失败 503)、`A2AClient.ts`(fetch 薄封装: getAgentCard/listAgents/discoverAgents/createTask/getTask/cancelTask/pushNotification, 出站 api-key/bearer 头注入+超时 AbortController)、`index.ts`(统一出口)。
- `src/main.ts` 注册 `registerA2ARoutes(app)`(紧邻 `registerAdminRoutes`)。
- `AgentRegistry.ts` 内存版 `A2AProtocolManager` 的 `@deprecated` 注释更新为指向 `src/a2a` 薄壳(文档宣称与实情一致)。
- 验收验证(本机 jest 不可用, 用 tsc 转译真实源码 + Module._load 桩驱动): `A2ARouter` 转发 8/8 通过(POST 路径/方法/体/鉴权透传、GET 发现端点、上游不可达 503); `A2AClient` 6/6 通过(getAgentCard/api-key 头/createTask URL+POST+体/cancelTask URL)。冒烟测试 `tests/unit/a2a/a2aRouter.test.ts` 已写入(真实 jest 环境可跑)。
- 要点: TS 不实现任何 A2A 业务逻辑, 全在 Python `agent/a2a/`; 对齐 AGENTS.md §0.1。

## P1-2 统一执行 Action 抽象(完成)
- 新建 `src/harness/action/`:
  - `types.ts`: `ActionChannelKind`('tool'|'desktop'|'mcp')、`ActionRequest`、`ActionResult`(含可选 `verification`)、`VerificationOutcome`(Python VerificationResult 归一化投影)、`ActionChannel` 接口。
  - `channels/ToolChannel.ts`(包 ToolRegistry)、`channels/DesktopChannel.ts`(包 DesktopActionExecutor + 可选 verify 接回)、`channels/McpChannel.ts`(经 `getActivePythonBridge().callMcpTool`, 兼容 `mcp_{server}_{tool}` 与 `{server}/{tool}`)。
  - `verify/VerificationBridge.ts`: `VerificationBridge` 接口 + `PythonVerificationBridge`(HTTP → `PYTHON_AGENT_URL/v1/perception/verify-action`) + `LocalVerificationBridge`(兜底)。
  - `ActionDispatcher.ts`: 单一路由入口 `dispatch(req)` + 便捷 `verifyDesktopAction(...)`; `configureActionDispatcher({toolRegistry, desktopExecutor, verifier})` 启动装配。
  - `index.ts` 统一出口。
- 接线: `AgentHarness` 两处初始化后 `configureActionDispatcher({ toolRegistry: this.toolRegistry })`(tool 通道拿到真实注册表, 非破坏性); `desktop_automate` 工具加 `verify` 可选参数, 执行后 `getActionDispatcher().verifyDesktopAction(...)` 把验证结果写回 ToolResult.metadata(桌面动作接回 action_verifier, 闭环)。
- Python 侧新增 `agent/api/action_verify.py`: `POST /v1/perception/verify-action`(pydantic 体 → `ActionVerifier.verify`, 异常优雅降级 200+success=false); `main.py` 注册 `action_verify_router(prefix="/v1/perception")`。`tests/test_action_verify.py` 3 例通过。
- 验证: Python pytest 3/3; TS 用 tsc 转译真实源码+Module._load 桩运行时验证 17/17(tool/desktop/mcp 三通道归一 + 路由 + 验证桥归一 + 错误降级)。
- 要点: 校验核心 ActionVerifier 在 Python, TS 仅桥接/归一, 对齐 AGENTS.md §0.1。

## P1-3 听觉真正可用(完成)
- 新建 `src/multimodal/SpeechSynthesizer.ts`: `TTSBackend`('mock'|'real') 切换; real 后端未注入 `realSpeak` 时显式报错(fail-closed), 不再静默降级; 后端优先级 `ttsBackend参数 > deps真实合成器 > env TTS_BACKEND > mock`。
- 改 `src/harness/tools/system/voice_interact.ts`: 删除 `handleListen` 中硬编码 `Buffer.from('模拟音频输入数据')`; 新路径优先级 `audioPath(读文件真实ASR) > 注入 audioCapturer(麦克风) > fail-closed`; 真实 ASR 经 `SpeechRecognizer.recognize`(其已自动写 `perception_update` 事件), 并把识别文本经 `EventBus.emit('voice_recognized', ...)` 写入感知总线 + 回灌 `interactionEngine.handleUserSpeech`(若装配)。`handleSpeak` 改用 `SpeechSynthesizer` 后端切换; 新增 `audioPath`/`ttsBackend` 参数与 `VoiceInteractDeps.audioCapturer`; `VOICE_INTERACT_DEF` 补参数。
- 验证: TS 用 tsc 转译真实源码 + Module._load 桩运行时验证 8/8(listen 真实ASR+感知总线事件 / 无音频源 fail-closed / 麦克风采集器 / speak mock / speak real 无后端报错 / speak real 注入合成器); jest 冒烟 `tests/unit/harness/voiceInteract.listen.test.ts` 已写(真实 jest 环境可跑)。
- 要点: SpeechRecognizer 内部已经把 ASR 进度/结果发到 `EventBus`(感知总线), 故"ASR 结果写入感知总线"由真实识别链路天然满足; 本机无麦克风/Whisper, 真实音频识别需在带麦克风+Python Whisper 环境跑通。

## 九大能力模块全面审计(完成, 仅审计+方案, 未改代码)
- 对象: 能记(memory_store/recall/search)·能搜(web_search)·能抓(web_fetch)·能说(tts_speak)·能画(image_generate)·能写(code_generate/review/fix)·能管(task_manage/note_take/calendar)·能思(emotion_detect/self_reflect)·能控(shell_exec/file_read/file_list), 14 工具全部已注册并纳入默认 toolset(registerHarnessTools.ts:386-587; builtinToolsets 映射)。
- 方法: 实时探测(分 5 路 Explore 子代理逐文件读 + 主代理亲自核验 P0)。交付: `jiabaixing_capability_audit_2026-08-12.html`。
- **P0 系统性风险(已亲自核验)**: ① file_read.ts:99-101 / file_list.ts:78-80 绝对路径无项目根 containment → 沙箱逃逸; ② ToolRegistry.execute:254-305 仅 Schema+Permission, 不调 ApprovalEngine, needsConfirmation 被忽略, setPreChecksEnabled 可关全部门禁; ③ execute:313-322 Promise.race 超时只 reject 不取消, execSync 同步阻塞使超时失效。
- **P0 安全/正确性缺陷**: web_fetch.ts:140 SSRF 仅校验 https? 缺 localhost/169.254/内网(与 Python async_is_safe_url 分裂); shell_exec.ts:217-226 禁令牌子串匹配可绕过; emotion_detect.ts:37-57 / self_reflect.ts:78-132 无 try-catch、无 deps 守卫、无依赖时假成功; 能思 TS 侧为规则/伪反思而 Python 同类真实调 LLM(能力不对称)。
- **P1**: memory_store 缺依赖静默成功(:170-206); code_fix 声明 FILE_WRITE+requiresConfirmation 却从不写盘(:95-106); code_review LLM 失败被 catch{} 吞(:94); calendar 无 deps 守卫+提醒不实+零测试; task/note/calendar 并发 read-modify-write 无锁; image_generate/tts_speak 无费用护栏; 无瞬态重试、自愈未接入 TS、无并发/成本预算。
- **测试盲区**: code_generate/review/fix 三者无 executor 单测; calendar 零测试; file_read/file_list/web_fetch/memory_store/memory_search/能思 仅 e2e 无单元。
- 方案: 安全加固(P0)/错误恢复重试+熔断+真超时(P1)/资源缓存+配额+异步化(P1)/能力协同 D1-D4(P2)/架构归一 F(谁为主、能思对齐、去静默成功)。路线图分 4 轮。已请用户确认是否实施 P0 第1轮。

## P0 第1轮安全加固(已实施并验证)
- 用户确认实施 P0 第1轮(文件沙箱 containment / web_fetch SSRF / 执行层审批强制 / Shell 禁令牌硬化), 低风险高确定性。
- **文件沙箱 containment**: `file_read.ts` 新增 `resolveWithinRoot(rawPath, projectRoot?)`(项目根+os.tmpdir+USERPROFILE/HOME 三允许根, 越界抛错), 接入执行器(deps.readFileContent 与本地 fs 两路径均走 safePath); `file_list.ts` 复用之并接入 listWithFs 与注入分支(均传 safeDir)。修复 P0-① 沙箱逃逸。
- **web_fetch SSRF**: 新增 `isSafeUrl`(拒绝 localhost/127/::1/169.254.169.254/10/172.16-31/192.168/0.0.0.0/100.64 CGNAT, IPv6 回环/链路本地/唯一本地, 十进制/十六进制/八进制编码 IP, 百分号编码主机名, 非 http(s) 协议)。接入执行器两路径。修复 P0 SSRF 分裂。
- **执行层审批强制**: `ToolRegistry.execute` 在 PermissionGuard `needsConfirmation` 为真时(ask 策略工具 + high/critical)调用 `ApprovalEngine.requestApproval`; 无审批方/异常 fail-closed 拒绝; 保留 `setPreChecksEnabled` 门控。修复 P0-② needsConfirmation 被忽略。
- **Shell 禁令牌硬化**: `shell_exec.ts` 新增 `isShellCommandDangerous`(归一化去引号/转义+分词+危险令牌组合: 危险命令名/编码 PowerShell/cmd /c/递归删除危险目标/递归 chmod 777//根/taskkill 系统进程/kill -9 1/注册表/net user/cacls 全员/mklink /h/fork bomb/设备写入), 保留原 FORBIDDEN_COMMANDS 作归一化子串兜底。覆盖 `rm -r -f /`、`rm"-rf" /` 等绕过。修复 P0 子串绕过。
- **验证**: 本机 jest 不可用, 用 tsc 转译真实源码 + Module._load 按 basename 拦截 Logger/types 桩, 跑 32 项断言全部通过(4 越界文件/14 SSRF/14 禁令牌)。ToolRegistry+ApprovalEngine 转译 emit 成功(仅隔离编译的 @types/兄弟导入噪声, 非本次改动)。
- 5 文件改动: file_read.ts / file_list.ts / web_fetch.ts / ToolRegistry.ts / shell_exec.ts。

## P0-③ shell_exec 超时取消 + restart 精确规则(已实施并验证)
- 续 P0 第1轮余下两项(用户显式要求): ① `execSync` → `spawn`+`kill` 真正超时取消; ② `restart` 子串误杀改为精确规则。
- **P0-③ 超时取消修复**: `shell_exec.ts` 移除 `execSync` 导入, 新增 `runSpawn(command, timeout, cwd)`(spawn shell:true + 收集 stdout/stderr + 超时先 SIGTERM 后 500ms SIGKILL), 执行器原 `execSync` 回退块改为 `await runSpawn`, 超时返回 `命令执行超时(>Nms)已被取消`, 非 0 退出码走 fail。修复审计 P0-③ "execute Promise.race 超时只 reject 不取消, execSync 同步阻塞使超时失效"。
- **restart 精确规则**: 从 `FORBIDDEN_COMMANDS` 与 `EXACT_DANGEROUS_CMDS` 移除过宽 `restart`, 新增显式电源/重启拦截(`reboot`/`restart`(裸)/`halt`/`poweroff`/`shutdown`), 放行服务重启(`systemctl restart <svc>`/`sc restart <svc>`/`service <svc> restart` 命令名非 restart 不命中)。顺带补 `mkfs.ext4` 等变体拦截(`cmdName.startsWith('mkfs.')`), 修掉原 `mkfs` 仅拦裸命令的缺口。
- **验证**: tsc 转译真实源码 + Module._load 桩, 54 项断言全过: 服务重启 3 例放行(`systemctl restart nginx`/`sc restart Spooler`/`service nginx restart`); 电源命令 6 例拦截(reboot/restart/shutdown /r//s/poweroff/halt); 破坏性/绕过 38 例仍拦(含 `rm -r -f /`、`rm"-rf" /`、`mkfs.ext4 /dev/sda1`); runSpawn 快命令(`echo hello`)成功 exitCode=0, 挂起命令(`ping -n 6 127.0.0.1` 配 300ms 超时)被取消(error 含"超时"且耗时<4s, 证明子进程真被 kill 而非阻塞)。
- 仅 1 文件改动: `src/harness/tools/system/shell_exec.ts`(相对基线 +372/-21, 含本轮与首轮禁令牌硬化累计)。

## 能力路线图 A-F 现状核验(实时探测, 非采信文档)
- 用户提供详细 A-F 路线图(安全/错误恢复/资源/协同/测试/架构一致性)。本次按"不采信文档、实时探测"核验:
- **已完成**: A1 文件 containment(file_read/file_list resolveWithinRoot)、A2 SSRF(web_fetch isSafeUrl + 默认 fetch 有 AbortSignal.timeout(283))、A3 审批强制(ToolRegistry→ApprovalEngine)、A4 禁令牌(isShellCommandDangerous)、F3 去静默成功、B4-shell(spawn+kill)。
- **待办(按表下一轮)**:
  - F2 能思对齐: `emotion_detect.ts` 无 try-catch/deps 守卫(缺依赖抛 TypeError); `self_reflect.ts` 缺依赖时假成功(返回"已记录反思"却未记录), 二者皆规则伪反思非真实 LLM。
  - B1 重试 / B2 自愈 / B3 熔断 / C2 并发配额: `ToolRegistry.ts` 无 retry/circuit/semaphore(实测 grep 0 命中)。
  - C1 结果缓存: `result_cache.ts` 已存在但未接入 execute 包装层。
  - C3 流式截断 / C4 异步文件读: 未做。
  - E 测试补齐: 仅 `tests/unit/tools/code_review_project.test.ts` 一个 project 测试; code_generate/review/fix 与 calendar **无 executor 级单测**。
  - F1 canonical 选型(TS vs Python 双端归一)、D1-D4 能力协同(P2): 未启动。
- **关键待决策(F2 能思对齐路径)**: ①经 PythonAgentBridge 调 Python LLM(与 Python 能力一致, 符合 AGENTS §0.1) ②TS 保留规则模式但 DEF 显式标注"轻量规则"且降级诚实 ③暂缓。需用户拍板。
- 结论: A 安全段与 F3 已闭环; 下一可执行轮为 P0 第2轮余下(F1/F2 + code 三件套单测)或 P1 第3轮(弹性护栏)。

## P0 第2轮余下(已实施并验证) — F2 能思对齐 + code 三件套单测
- 用户决策: 下一轮=P0 第2轮余下; F2 能思走"TS规则+诚实降级"(保留 TS 规则实现, DEF 显式标注"轻量规则", 缺依赖诚实失败而非假成功)。
- **F2 emotion_detect**: 加 deps 空守卫 + try-catch; 缺 `detectEmotionFromInput` 或抛错→诚实 `success:false`(不再崩溃/假成功); DEF 追加"轻量规则模式"标注。
- **F2 self_reflect**: 修复假成功——`agentSelfReflection` 与 `reflectionStore` 均缺失时返回 `success:false` + "未实际记录"(原实现谎称"已记录反思"); 有后端则正常持久化; DEF 追加"轻量规则模式"标注。
- **code_fix 诚实落地修正**: 移除误导的 `Permission.FILE_WRITE`(声明却从不写盘), `requiredPermissions` 改为仅 `[CODE_EXECUTE]`; DEF 明确"轻量规则模式: 返回修复预览, 不自动写盘"; 保留 `requiresConfirmation`。消除"只建议不落地"错位。
- **code 三件套 executor 单测(E)**: 新建 5 个 jest 文件 `tests/unit/tools/{emotion_detect,self_reflect,code_generate,code_review,code_fix}.test.ts`, 覆盖错误路径(缺依赖/文件不存在/LLM 失败/抛错)与安全路径(硬编码密钥 critical/==→===/var→const/FILE_WRITE 移除)。code_generate 早已诚实(缺 generateCode 即失败), code_review 已健壮(规则+安全+LLM 分层, 文件不存在失败)。
- **验证**: 本机 jest 不可用, 用 tsc 转译 5 文件真实源码 + types 递归代理桩做运行时验证, **33/33 全过**(emotion 缺依赖失败/注入成功/抛错失败; self_reflect 无后端失败/有后端持久化; code_generate 缺实现失败/注入成功/抛错失败; code_review 文件不存在失败/硬编码密钥 critical/空文件无问题; code_fix var→const/==→===/注入成功/抛错失败/权限仅 1 个+不自动写盘标注)。
- **改动文件**: 源 `emotion_detect.ts`/`self_reflect.ts`/`code_fix.ts`(3); 新增测试 5 个。未提交(用户未要求)。
- 仍待办(后续轮): B1 重试/B2 自愈/B3 熔断/C2 并发配额(ToolRegistry 包装层); C1 结果缓存接线; C3 流式; D1-D4 能力协同(P2); F1 全量 canonical 选型(web_search/file_*/shell_exec TS vs Python 双端归一)。

## P1 第3轮 ToolRegistry 统一弹性包装层(已实施并验证) — B1 重试 / B2 自愈 / B3 熔断 / C2 并发配额
- 用户决策: 续 P1 第3轮 = ToolRegistry 统一执行包装层(错误恢复+资源护栏)。实时探测发现: ToolRegistry **已有** P2-4 熔断器(`circuitBreakers`/`updateCircuitBreaker` 已接线, 阈值 5/60s)与 P1-4 按环境变量重试(full jitter), 故 B3 基础已存在、B1 仅缺错误类型分类。
- **关键发现( preexisting 运行时崩溃)**: `execute()` 中 `const permResult` 声明在 `requiredPermissions.length>0` 的 if 块内, 但块外第 404 行 `permResult?.needsConfirmation` 引用之 → **任意无 requiredPermissions 的工具调用 execute() 即抛 ReferenceError**(= 绝大多数工具)。本次修复为外层 `let permResult: ...|undefined`, 否则我的新代码根本跑不到。属真实炸裂级 bug, 之前被"文档宣称"掩盖。
- **B1 重试分类**: 新增 `computeMaxRetries`(网络/LLM 类默认 3、其他默认 0, env TOOL_RETRY_<name>/DEFAULT 可覆盖, 上限 3) + `isRetryableError`(RETRYABLE_ERROR_RE 仅匹配 429/5xx/ETIMEDOUT/网络抖动); 非瞬态错误立即 break 不重试。修正前"任意失败都重试"。
- **B3 熔断补全**: `runSingleAttempt` 抽取单次执行(超时竞速+标准化+可靠性记录+熔断更新); **抛出型异常路径也调 `updateCircuitBreaker(name,false)`**(原仅成功路径更新, 吞噬型异常绕过熔断)。
- **B2 自愈**: 新增 `setSelfHealHandler`(由 Python 经 bridge 注入, 符合 §0.1: TS 仅壳) + `maybeSelfHeal`(PARAM_FIX→重执行修正参数 / ALT_TOOL→委托替代工具 / DEGRADE→诚实降级 success); 未注入或自愈失败→诚实返回原失败(不假成功)。
- **C2 资源护栏**: ① 每-agent 信号量 `MAX_CONCURRENT_TOOLS`(默认 4, env TOOL_MAX_CONCURRENT); ② 付费工具(image_generate/tts_speak/web_search)会话级每日配额(env TOOL_QUOTA_<tool>/DEFAULT 默认 50); ③ 同参去重(`dedupKey`=日期+工具+参数 sha256, 命中返回缓存, 不重复计费)。注明: 配额/去重为内存态, 多副本需迁 Redis(对齐生产就绪文档)。
- **验证**: 本机 jest 不可用, 用 tsc 转译真实源码(`--noResolve` 隔离)+ Module._load 按 basename 拦截 PerformanceMonitor/Logger/types/ApprovalEngine + 全局 SchemaValidator/PermissionGuard 桩, 跑 `.p1_verify/verify.cjs` **20/20 全过**(B1 不可重试/网络重试4次/非网络不重试; B3 5次后 open+拒绝; C2 配额超2拒绝+去重命中; B2 修正后成功+无注入诚实失败; 信号量≤4)。jest 冒烟 `tests/unit/tools/ToolRegistry.resilience.test.ts` 已写(CI 可跑)。
- **改动文件**: 仅 `src/harness/tools/registry/ToolRegistry.ts`(新增常量/枚举/字段/方法 ~200 行 + permResult 作用域修复); 新增测试 1 个。未提交(用户未要求)。
- 待办(后续轮): C1 result_cache 接 execute; C3 流式截断; D1-D4 能力协同(P2); F1 全量 canonical 选型。

## P2 第4轮 能力协同(已实施并验证) — C1 结果缓存接线 / C3 流式截断 / D1-D4
- 用户指令: "启动 P2 第4轮(C1 结果缓存接线、C3 流式截断、D1-D4 能力协同)"。实施为对既有 execute 包装层/工具的非破坏性增量。
- **C1 结果缓存接线**: 实时探测确认 `ToolCallGuard` 此前为死代码(仅测试+Python engine.py 引用, 未接入 TS 运行时)。现 `ToolRegistry` 新增 `private callGuard = new ToolCallGuard()`, 对 `idempotent && category∈{NETWORK,FILE,MEMORY} && name!=='result_cache'` 的工具在审批后先做 `guard()` 短路(命中返回缓存不重执行), 成功路径 `record()` 写回(TTL 5min/去重窗 30s)。修复审计"C1 存在但未接线"。
- **C3 流式截断**: 新增 `truncateToolOutput(result)`(默认 ≤8000 字符, env `TOOL_OUTPUT_MAX_CHARS` 可配), 包装层成功分支 + C2 去重命中 + C1 缓存命中三处均调用, 填充 `metadata.truncation={truncated,truncatedLength,originalLength}` 并加 `...[输出已截断]` 后缀, 防上下文窗口撑爆/缓存膨胀。
- **D4 capMetrics 激活**: 新增 `recordCapability` 在每次执行(成功/缓存命中)后按 `ToolCategory` 调 `capMetrics.record`。实时探测发现 capMetrics 此前**零调用站点**(死代码)。**诚实修复(潜在编译错误)**: 原 `record(key: MetricKey, ...)` 的 `MetricKey` 是 5 字面量闭并集, `ToolCategory` 为字符串枚举不可赋值 → 真实 `tsc` 门禁会报错。已扩展 `MetricKey` 加九大能力类别(memory/file/code/desktop/cognition/system/daily/network/perception, 向后兼容原 5 键)并将 `record` 形参放宽为 `string`(未知 key 静默 `if(!c) return`)。
- **D2 认知总线**: `src/shared/eventTypes.ts` `CoreEvents` 新增 `cognition_result` 事件; `execute` 在认知类工具完成后 `EventBus.emit('cognition_result', {tool,category,success,durationMs,outputPreview,error,timestamp})`。注: 真实 LLM 回灌 ReAct 为后续(当前认知工具为轻量规则模式, 本轮仅打通管线)。
- **D1 knowledge_query RAG 闭环**: `knowledge_query.ts` 在 `results.length===0 && deps.webSearch` 时降级 `webSearch`(推 `🌐 网络检索补强` 行)并把结果经 `deps.memoryStore(query, combined)` 回填记忆; 无 `webSearch` 时诚实返回"没有找到…"。`registerHarnessTools.ts` 注入 `webSearch`(searchEngine 适配)/`memoryStore`(storeShortTermMemory 适配)。
- **D3 代码安全扫描**: 新建 `src/harness/tools/code/codeShared.ts` 纯函数中间件 `scanGeneratedCode(code)`(SECRET_PATTERNS: AWS/Google/Slack/JWT/私钥/凭据赋值, 命中即掩码; 括号/方括号/花括号配平); `code_generate.ts`/`code_fix.ts`(LLM 成功路径 + LLM 不可用 performBasicFix 兜底路径)均把 `securityWarnings`/`secretHits` 写入 `metadata`。
- **验证**: ① 运行时 harness(`.p2_verify/verify.cjs`, tsc 转译 + Module._load 桩) **19/19 全过**(C1 缓存命中不重执行 / C3 截断≤8000 / D4 capMetrics 调用 / D2 cognition_result / 负向非幂等不缓存 / D1 降级+回填+诚实空 / D3 密钥检测)。② 修复后重跑仍 19/19。③ **真实 tsc 门禁核验**: 完整 `tsc --noEmit -p tsconfig.json` 因本机 `node_modules` 损坏缺 `@types/node`/`@types/jest` 报 2 个环境错误(均 `Cannot find type definition file`, 非本次改动、CI 有真实类型即绿); 隔离编译(`types:[]`)确认 `capMetrics.record(ToolCategory.X, true)` 零类型错误, 证明 D4 类型修复正确。④ jest 冒烟 `tests/unit/tools/ToolRegistry.p2.test.ts` 已写(镜像 19 断言, CI 可跑; 本机 jest 不可用)。
- **清理**: 临时 `.p2_verify/` 已删除(jest 文件为持久 CI 镜像)。
- **改动文件**: `ToolRegistry.ts`(C1/C3/D4/D2 + recordCapability/truncateToolOutput)、`CapabilityMetrics.ts`(MetricKey 扩展+record 放宽)、`eventTypes.ts`(cognition_result)、`knowledge_query.ts`(D1)、`registerHarnessTools.ts`(D1 注入)、`codeShared.ts`(新建 D3)、`code_generate.ts`/`code_fix.ts`(D3 接线)、`tests/unit/tools/ToolRegistry.p2.test.ts`(新建)。未提交(用户未要求)。
- **路线图清算**: A-F 中 A 安全段、B1/B2/B3/C2 弹性护栏、C1/C3 资源、D1-D4 协同、E 测试补齐、F2/F3 一致性均已闭环。剩 **F1 全量 canonical 选型**(web_search/file_*/shell_exec 等 TS vs Python 双端归一)与 **D2 真实 LLM 回灌 ReAct** 为后续独立轮次, 待用户拍板。

## D2 真实 LLM 回灌 ReAct(已实施并验证) — 跨语言认知信号管线闭环
- 用户拍板: 续 D2(放弃 F1, 因 Python web_search/file_*/shell_exec 能力不对称 shll_exec 安全模型未定)。目标: TS 认知工具(emotion_detect/self_reflect/scene_analyze)完成 → 经 EventBus 发出 cognition_result(携带 sessionId) → TS 订阅者转发 PythonAgentBridge.sendCognitionSignal → Python POST /v1/cognition/signal → Python ReAct 每轮 LLM 调用前把会话级认知信号注入上下文(元认知回灌)。
- **TS 侧**: ① `ToolRegistry.runSingleAttempt` D2 块从 `context.sessionId ?? context.metadata.sessionId` 捕获会话标识写入 cognition_result payload(`src/harness/types.ts` 新增 `sessionId?: string`); ② 新建 `src/harness/cognition/cognitionForwarder.ts`(`registerCognitionForwarder` 幂等订阅 cognition_result, 仅当 payload 有 sessionId 才经 `getActivePythonBridge()?.sendCognitionSignal` 转发, 失败 `.catch` 静默降级); ③ `PythonAgentBridge.sendCognitionSignal` POST `/v1/cognition/signal`(debug 级失败日志); ④ `src/server/bootstrap.ts` 在 `setActivePythonBridge` 后 `registerCognitionForwarder()`; ⑤ `eventTypes.ts` cognition_result 增 `sessionId`。
- **Python 侧**: ① 新建 `python/agent/core/cognition_buffer.py`(纯模块无重型依赖: `store_cognition_signal` 滚动保留每会话最近 10 条; `inject_cognition_into_messages` 把缓冲拼成 system 消息插到首个非 system 消息前); ② 新建 `python/agent/api/cognition.py`(`POST /v1/cognition/signal`, 缺 session_id 返回 success=false, 异常优雅降级); ③ `main.py` 注册 `cognition_router(prefix="/v1")`; ④ `conversation_loop.py` `run()`(L373 后)与 `run_stream()`(L946 后)两处注入会话级认知信号到当轮 messages(注入一次, 失败 log.warning 跳过不阻断主链路)。
- **验证**: ① TS 运行时 harness(`.d2_verify/verify.cjs`, tsc 转译真实源码 + Module._load 按 basename 桩 EventBus/bridgeRegistry/Logger) **10/10 全过**(订阅+幂等 / sessionId 转发+参数映射 / 无 sessionId 丢弃 / bridge=null 不崩 / rejected 被 .catch 吞+debug 降级); ② Python pytest `test_cognition_buffer.py` **7/7 全过**(缓冲+滚动+注入位置+API 存储注入+缺 session_id 拒绝); ③ jest 冒烟 `tests/unit/harness/cognition/cognitionForwarder.test.ts` 已写(镜像 10 断言, CI 可跑; 本机 jest 不可用)。
- **清理**: 临时 `.d2_verify/` 已删除(jest 文件为持久 CI 镜像)。
- **改动文件**: TS `cognitionForwarder.ts`(新)/`ToolRegistry.ts`(sessionId 捕获)/`types.ts`/`eventTypes.ts`/`PythonAgentBridge.ts`/`bootstrap.ts`; Python `cognition_buffer.py`(新)/`cognition.py`(新)/`conversation_loop.py`(两注入点)/`main.py`(注册)/`test_cognition_buffer.py`(新); 测试 `cognitionForwarder.test.ts`(新)。未提交(用户未要求)。
- **现状清算**: A-F 路线图中, F1 全量 canonical 选型(web_search/file_*/shell_exec TS vs Python 双端归一)为唯一剩余独立轮次。D2 真实回灌已闭环(认知信号管线打通, 注入 ReAct LLM 上下文); 真实效果取决于用户把 TS 认知工具接到真实会话上下文(sessionId 透传)与 Python Whisper/LLM 环境。

## 用户二次能力审计复核(实时探测, 发现快照已过期)
- 用户再贴一份更细的能力审计(7 域 ~35 项 P0/P1/P2, 含行号证据)。**经实时复核发现该快照已过期**——其 P0 层几乎全部已被本会话前序轮次(P0第1轮/第2轮/P1第3轮/P2第4轮/D2)实际修复。
- **P0 层复核结论(Explore 子代理逐文件读 + 主代理亲核验 web_fetch/shell_exec)**:
  - #1 web_fetch SSRF 缺失 → REFUTED: `isSafeUrl`(network/web_fetch.ts:135-238)已落地 localhost/内网/169.254/CGNAT/IPv6/十进制|十六进制|八进制编码; 文件已从 `system/` 迁 `network/`。**残留真风险**: `normalizeNumericHost`(:200-214)仅处理"全数字/0x/0"整主机, 混合编码 `0x7f.0.0.1`(首段 hex 余 decimal)不匹配任一分支 → 落入 :196 `return true` 放行 = SSRF 绕过(真实待修)。
  - #2 httpClient 无超时/全量缓冲 → PARTIAL: SSRF 已在 :261 修复; 但 `deps.httpClient.get(url)`(:275)无 `AbortSignal`(25s 仅作用于自管 fetch 分支 :283), 且两路径 `response.text()`(:293)整响应入字符串(内存 DoS)。**两项残留真风险待修**。
  - #3 shell 禁令牌子串绕过 → REFUTED: `isShellCommandDangerous`(system/shell_exec.ts:106-280)已归一化+分词+结构化判定, `rm -r -f /`/`rm"-rf" /`/`del /q /s C:\` 均拦; F1 桥可用时再代理 Python 白名单(双重防护)。
  - #4 file 沙箱逃逸 → REFUTED: `resolveWithinRoot`(file/file_read.ts:76-100, file_list.ts:151)已落地 containment; `..`/绝对越界均拒。**残留**: 词法判定不跟随 symlink, 允许根内 symlink 指向根外可逃逸(次级)。
  - #5 能思 TS规则 vs Python LLM 不对称 → CONFIRMED 但属设计取舍: P0第2轮用户已拍板"TS规则+诚实降级", 非缺陷。
  - #6 emotion_detect 无守卫/try-catch → REFUTED: F2 已加 deps 空守卫+try-catch, 缺依赖诚实 `success:false`(emotion_detect.ts:44-82)。
  - #7 code 三件套无 executor 单测 → REFUTED: P0第2轮已建 5 个 jest(`code_*/emotion_detect/self_reflect.test.ts`)。
- **P1/P2 层未复核(很可能也部分过期)**: code_fix 声明 FILE_WRITE 却从不写盘 → P0第2轮已修(移除 FILE_WRITE, 标注"轻量规则: 返回预览不自动写盘"); image_generate 无费用护栏 → P1第3轮 ToolRegistry 已加付费工具会话配额/去重; 仍可能 OPEN: memory_store 缺依赖静默成功、calendar 无 deps 守卫+零测试+提醒不实、tts_speak 无合成器假成功、task/note/calendar 并发 read-modify-write 无锁、web_search 未注入引擎回退 TS 本地(与 F1 冲突)。
- **结论**: 该审计快照过期, 勿盲目按表修 P0(多数已闭)。下一步建议: ①修 3 项真实残留(SSRF 混合编码/ httpClient 超时+缓冲/ symlink containment); ②对 P1/P2 实时复核得真值再规划。待用户拍板。
