## 2026-06-29 家百星 Agent 架构审计

### 完成的工作
- 深入理解项目混合架构：TS 薄网关 (:3111) + Python FastAPI 后端 (:3112)
- 完整审计架构：发现 P0 严重问题 2 个、P1 重要问题 2 个、P2 关注问题 5 个
- 核心发现：
  1. 30+ TS 文件标记 @deprecated 但仍存活（双轨代码库）
  2. 三层路由决策粗糙（关键词匹配）
  3. 伪流式输出（非真正的 token 级流式）
  4. 5 套上下文系统碎片化
- 输出 agent_audit_report.html：含 4 维度 16 项优化方案 + 3 阶段路线图
- 优化重点：流式输出、工具可视化、自适应路由、优雅降级、统一上下文、记忆召回

### 2026-06-29 下午-晚间：16项代码优化实施

#### Phase 1/2 实施 (14项)
- P0-1: 真正的流式输出 — ws_root async generator + token级事件
- P0-2: 工具调用过程可视化 — tool_start/tool_end WebSocket事件
- P0-3: 自适应路由决策升级 — 加权评分替代关键词匹配
- P0-4: 全局超时控制 + LLM退避重试 — asyncio.wait_for + 指数退避jitter
- P1-1: 任务进度可见性 + 取消支持 — cancel_token (asyncio.Event)
- P1-2: 统一上下文管道 — UnifiedContextOrchestrator替代碎片化搜索
- P1-3: 记忆召回质量 — 衰减(7天内1.3x)+知识图谱增强(0.7x)
- P1-4: 思考过程可见 — ThinkScrubber分离think内容
- P1-5: 错误信息人性化 — 13种技术错误→中文友好建议
- P1-6: 澄清式交互 — intent_confidence < 0.6触发澄清
- P1-7: 执行轨迹回放 — GET /v1/trajectory API
- P2-1: 代码清理 — DEPRECATION_MAP.md记录30+废弃TS文件
- P2-4: 会话搜索+书签 — 5个新REST端点
- P2-5: 性能仪表盘 — 延迟分布+健康状态+LLM指标

#### Phase 3 实施 (3项)
- #16 HTTP→WS通信优化:
  - axios连接池: http.Agent keepAlive + maxSockets=10
  - WS流式聊天通道: chatWs连接, processInputStream async generator
  - processInput优先WS回退HTTP, 指数退避重连
  - bootstrap.ts中connectChatWs()初始化
- #17 多模态输入补全:
  - MessageInput.tsx: 拖放onDragOver/onDrop + 剪贴板onPaste
  - CSS: .drop-overlay动画提示 + .dragging虚线边框
  - coreRoutes.ts: multipart上传端点(50MB) + /api/files/:filename访问
  - 简易multipart解析器(parseMultipart + parseHeaders)
  - JSON body limit 10MB→50MB
- #20 工具发现与推荐:
  - ToolDefinition扩展: tags/scenes/shortDesc/capabilityLevel字段
  - ToolRegistry新增: getByTags/getByScene/getByCapabilityLevel/filterBy组合过滤
  - SceneToToolsetMapper: 场景关键词加权检测+渐进式3级披露+环境感知
  - Executor接入: 动态工具集选择替代静态activeToolset
  - Python ToolDefinition同步更新

#### 修复
- acpRoutes.ts: pythonBridge变量未声明 → 添加模块级let声明
- Executor.ts: plan.originalInput不存在 → 改用context.metadata.input
- PythonAgentBridge: 2处类型断言修复

#### 审计
- TS编译: 新增改动零错误 (预存25个与本次无关)
- Python测试: 1477/1479通过 (2个预存失败)
- 审计报告: docs/AUDIT_REPORT_V5.md — P0无阻塞, P1有3项, P2有8项

### 2026-06-29 深夜：P1/P2修复 + E2E浏览器测试

#### P1修复（3项全部完成）
1. **P1-1 文件上传MIME白名单**: coreRoutes.ts 添加MIME_WHITELIST(图片/文档/音频/视频/代码)，扩展名回退映射，拒绝不安全文件
2. **P1-2 WS重连时序**: PythonAgentBridge.ts disconnect()清除重连定时器 + _disconnected标志
3. **P1-3 EventBus指数退避**: connectEvents()从固定5秒改为指数退避(1s→2s→4s→8s capped)

#### P2改进（8项）
- multipart迁移到busboy库（npm install busboy + @types/busboy）
- SceneToToolsetMapper支持外部JSON配置(config/scene_toolset_map.json)
- MessageInput添加React.memo包裹
- python/agent/utils/目录创建
- package.json添加check:ts脚本
- WS超时可配置(wsStreamTimeoutMs默认120s)
- TS编译零错误验证

#### E2E浏览器测试
- agent-browser + Chromium访问localhost:3100
- **前端必须craco start**（react-scripts缺@shared/contracts alias）
- 验证4阶段：初始渲染✅ → 输入✅ → 发送处理✅(规划阶段高亮确认) → 响应⏳
- 设计符合度约89%（前端交互层面）

### 2026-06-29 午夜：三入口全面E2E测试

#### 测试设计
- **网关API**: 11个场景 (健康检查/对话/工具调用×5/WS流式)
- **前端UI**: 3个场景 (服务状态/通信链路/组件结构)
- **CLI**: 5个场景 (ask/status子命令，验证LLM工具执行)

#### 关键证据：LLM确实执行了工具 🔥
- CLI `ask "读取 AGENTS.md..."` → LLM调file_read → 准确返回5角色(架构师/前端/后端/审计/测试)
- /api/process: intent="harness_orchestrated" → LLM自主调web_search
- WS流式: connected→processing→agent_execution×9→stream_start→23chunks→done

#### 结果
- 19个场景, 18通过, 1降级 → 95%通过率
- web_search/web_fetch/shell_exec/file_read/execute_code 5工具直调全通过
- 数据闭环: 输入→LLM→工具→结果→入口显示 全链路验证
- 报告: docs/E2E_TEST_REPORT_2026-06-29.md

#### 遗留问题（2026-06-30 凌晨全部修复）
- managed Node.js v22.22.2 npm损坏 → `npm install npm@10` → 10.9.8 ✅
- /v1/chat/completions未注册路由 → registerOpenAIRoutes() ✅
- CLI管道模式TTY检测需修复 → cli.ts条件重排 ✅
- better-sqlite3版本不匹配 → 确认正常无需修复 ✅

### 2026-06-30 凌晨：E2E问题修复

#### 修复内容
1. **P1 managed npm 损坏**: 在 managed Node.js 目录下 `npm install npm@10` 安装到 node_modules/npm/
2. **P2 OpenAI 兼容端点**: 
   - openaiCompatibleRoutes.ts 新增 `registerOpenAIRoutes(app, core)` 直接注册到 Express
   - main.ts 添加 import + setupRoutes 中调用
   - 验证: `/v1/chat/completions` → OpenAI 格式响应, `/v1/models` → model list
3. **P3 CLI 管道模式**: 
   - cli.ts 将 `!process.stdin.isTTY` 检测提前到子命令分发之前
   - 验证: `echo "test" | cli.js --json` → JSON 响应, 纯文本也正常
4. **P2 better-sqlite3**: 确认 managed Node.js v22.22.2 下模块正常加载，无需修复

#### 验证结果
- `/v1/chat/completions` ✅ — 返回标准 OpenAI completion 格式
- `/v1/models` ✅ — 返回模型列表
- CLI pipe: `echo "text" | cli.js --json` ✅ — JSON输出
- CLI pipe: `echo "text" | cli.js` ✅ — 纯文本输出
- 所有修改零 TS 错误（项目预存错误不计）
- 服务器已使用新代码重启
