# 2026-07-15 工作日志

## LLM 枢纽迁移完成（文档派生审计收口 · 第三枢纽）
- 按用户选定顺序 记忆 → LLM → MCP，LLM 枢纽于今日收口。
- 探针现状：**31 PASS / 0 FAIL（全绿）**，CI 红线 `process.exitCode=1` 已满足。
- 关键发现：Python `python/agent/api/llm.py` 早已完整（chat/工具/健康/流式/多模态/代码助手全有），`python/agent/llm/router.py` 的 `provider_manager`/`credential_pool` 提供多模型路由。TS 侧绝非"从零写 Python"。
- 改动（与 MemoryEngine 同范式：删 class 定义 + re-export 壳，使 `findDef(className)===0` → 探针自动 PASS）：
  - `src/models/LLMProvider.ts` → re-export 壳 `export { LLMProviderBridge as LLMProvider }`
  - 新建 `src/models/LLMProviderBridge.ts`（原 class 改名，bridge 优先 + 本地回退，`@deprecated`）
  - `src/models/MultiModelLLMProvider.ts` → re-export 壳 + 类型导出（`RoutingStrategy`/`RegisteredModel` 等）
  - 新建 `src/models/MultiModelLLMProviderBridge.ts`（原 class 改名，本地多模型路由保留为离线回退）
  - 注意 `class XBridge` 不被 `\bclass\s+X\b` 正则匹配，故 `findDef` 返回 0 自动 PASS
- 验证：
  - tsc：10 个预存错误（基线），LLM/MultiModel 相关 0 新增
  - jest（默认 run 内）：`LLMProviderCleanup` 9 passed、`JiabaixingCore` 30 passed、`ToTReasoning` passed
  - `tests/unit/desktop/DesktopAgentLoop*` 与 `tests/integration/*` 被 `jest.config.js` `testPathIgnorePatterns` 预设排除，与本次改动无关
- 下一步：MCP/记忆/LLM 三枢纽全绿，文档派生审计探针已清零。可收尾或复查其他文档断言（如 ARCHITECTURE 实时性）。

## 收尾整理 + 文档断言维度复查（同日续）
- **ARCHITECTURE.md 实时性更新**：头部加"架构现状速览(2026-07-15)"；新增 §1.6「枢纽迁移状态」表(覆盖 6 枢纽 + 验证闸门)；修正 §1.2(`LoopController.run()`→Python)、§1.3(memory/models 标 bridge 壳)、§3.1/§3.5/§3.9(旧 MemoryEngine/VectorDatabase 建议)。
- **LLM 枢纽 ADR**：新建 `docs/adr/ADR-001-llm-hub-migration.md`。
- **复查其他文档断言维度**：探针原仅覆盖 §0.1 的 6 行。扩展覆盖 §0.1 全部 11 行 + OTel，共 **37 条断言**。
  - 新覆盖且 PASS：Redis-Cache(真实 src 无 class)、MQ-MessageQueue(无独立类)、LLM-CredentialPool(`ProviderManager.ts` 整体 @deprecated)、及原有 31 条。
  - 新暴露 3 个 FAIL（**预存缺口，非本次回归**）：
    - `A-Persistence-Session`：`SessionStore` 在 `AgentHarness.ts:457` 仍 `new`，未 @deprecated；Python 侧已有 `session_store.py`。
    - `A-Persistence-Trajectory`：`TrajectoryDatabase` 在 `AgentHarness.ts:348` 仍 `new`；Python 侧已有 `trajectory.py`。
    - `A-OTel-SDK`：`src/monitoring/PerformanceMonitor.ts` 直接 `NodeSDK` + `bootstrap.ts:536` `initOTel()`，违背 §0.1「TS 仅透传」。
  - 处置待定：这 3 项触及 AgentHarness 运行时态 / bootstrap 行为，需用户决策（迁移 Python bridge / 标 @deprecated / 文档注明 §0.1 例外）。

## 收口 §0.1 三缺口（同枢纽范式，用户选"迁移 Python"）

- ** SessionStore **：`git mv SessionStore.ts → SessionStoreBridge.ts`；类改名 `SessionStoreBridge` 并标 `@deprecated`；新建 `src/persistence/SessionStore.ts` 重导出壳（`export * from './SessionStoreBridge'` + `export { SessionStoreBridge as SessionStore }`）。`AgentHarness.ts:457` 的 `new SessionStore()` 经壳零改动解析到 Bridge 回退。
- ** TrajectoryDatabase **：同理 `git mv → TrajectoryDatabaseBridge.ts`，类改名 + `@deprecated`，新建 `src/harness/persistence/TrajectoryDatabase.ts` 重导出壳（壳 `export *` 一并透传 `ExecutionRecord` 等类型，供 `TrajectoryQueryService.ts`/`TrajectoryFlywheel.ts` 的 `import type`）。
- ** OTel SDK **：`src/monitoring/PerformanceMonitor.ts` 删除 `@opentelemetry/sdk-node`/`exporter-prometheus`/`exporter-trace-otlp-http` 三 import、`DEFAULT_OTEL_CONFIG`、`otelSDK` 变量与 `initOTel()` 函数；保留 `perf` 本地指标 + `recordOTelRequest`/`getTracer`/`getMeter`/`startOTelSpan`/`shutdownOTel` 作为 traceId 透传壳（meter/tracer 默认 null，优雅降级）。`bootstrap.ts` 移除 `initOTel()` 调用与 import；`src/monitoring/index.ts` 移除 `initOTel` 重导出。
- **验证闸门全绿**：`node scripts/doc-derived-audit.mjs` → **37 PASS / 0 FAIL**（含上述 3 项由 FAIL→PASS）；`npx tsc --noEmit` 仍为预存 10 个无关错误（UnifiedContextPipeline/App.test/LongTermMemory/initHarness/WsProcessor），新代码零新增；`jest` monitoring 两件套 28/28 通过。
- ** ARCHITECTURE.md 同步**：§现状速览 + §1.6 枢纽迁移状态表新增 3 行（会话/轨迹持久化、可观测性），探针计数 31→37；探针脚本内联注释"预期 FAIL(待收口)"改为"已收口"。
