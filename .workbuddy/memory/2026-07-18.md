# 2026-07-18 工作日志

## 全面成熟度审计（对标《Agent 完整技术体系总图》）
- 交付：`docs/Agent_Technical_Maturity_Audit_2026-07-18.md`
- 方法：以用户给的框架（技术底座/三层运行/七维能力/MCP+A2A/落地要求）为清单，对 `python/agent/`、`src/`、`deploy/kubernetes/` 做代码实证。
- 关键实证结论（纠偏 2026-07-04 旧差距报告，已过时）：
  - Redis 缓存**已接线 MemoryEngine 主链路**（`engine.py:113/131/149/189/254`）；OTel 模块存在；K8s 2 副本+HPA+Redis+OTelCollector 清单完整；MQ(`RedisStreamsQueue`)+A2A(HTTP server+client+auth) 网络层齐备且 `main.py:110-130` 真实挂载 A2A、`/v1/mcp` 挂载 MCP。
  - **真差距收敛为 3 项**：① 分布式锁仅 configmap 配置、无实现（grep 零命中，2 副本下竞态隐患，P0）；② MQ 未成为执行主干（主链路仍进程内 asyncio.Queue，P0）；③ 真实线上商用闭环证据不足（P1）。
- 加权总分 ≈ 4.3/5（≈86%）：三层运行/七维能力/MCP+A2A 均已生产级甚至超前；短板正是博主强调的"底层生产硬核 + 落地要求"。
- 同步：按系统要求精简了被截断的 MEMORY.md。

## ② R1 管理面（运行时姿态 + 插件信任 HTTP 端点）
- 新增 `python/agent/security/runtime_control.py`：`RuntimeSecurityController` 单例，协调管理面 ↔ 真实执行器（写入推送到 `ApprovalManager.set_posture/set_lockdown` 与 `PluginTrustPolicy.set_trust`）。
- `python/agent/tools/approval_manager.py`：新增 `lockdown` 开关，`request_approval` 在锁定时立即拒绝全部工具调用（含 critical）——真实运行时硬门。
- `python/agent/plugins/trust.py`：新增 `list_plugins()` 与 `max_allowed_tool_risk(level)` 公共辅助。
- 新增 `python/agent/api/admin.py`（`/v1/admin`）：GET/POST `runtime/posture`、`runtime/lockdown`、`plugins/trust`，敏感写操作受 `AGENT_ADMIN_TOKEN` 保护（未配置则开发模式放行）。
- `main.py` 挂载 `admin_router`；`engine.py` 在 `_init_approval_manager`/`_init_plugin_manager` 把执行器 attach 到控制器。
- TS 薄网关：`src/server/routes/adminRoutes.ts` 代理 `/v1/admin/*` 到 Python(:3112)，`main.ts` 注册 `registerAdminRoutes`。
- 测试：`test_runtime_control.py`(4) 全绿；`test_admin_api.py`(4) 用 TestClient，本机无 fastapi 时 skip、CI 装齐依赖后运行。
- 验证：控制器单测 4/4；fastapi 缺时 admin_api 跳过（CI 跑）；`main.py` 因同 env 缺 fastapi 本机无法 import（预存环境缺口，CI 解决）。

## ③ R2 → EvolutionEngine（工具集采样喂送进化引擎）
- `engine.py`：`select_openai_tools_for_input`（真实 LLM 路径）采样选中工具集后调用 `_feed_evolution_toolset`，每次选择事件喂一次。
- `evolution/engine.py`：新增 `record_toolset_selection(scene, toolset_id, tool_names)`；`record_signal` 增加 `SignalType.TOOL_SELECTION_QUALITY` 分支，累计每工具 `selection_count`（供"场景→工具集"分布学习）。
- 测试：`test_evolution_toolset_feed.py`(3) 全绿（直测 + 引擎集成 + 关采样不再喂送）。
- 回归：`test_evolution.py` 通过；`test_evolution_orchestrator.py` 的 11 失败均为本机缺 pytest-asyncio 的预存 async 环境问题，与本次改动无关。

## ④ OAuth 授权码流转 + 令牌持久化
- 重写 `python/agent/llm/oauth_credentials.py`：通用 `OAuth2Config` + PKCE + `build_authorization_url`/`exchange_code_for_token`/`refresh_access_token`（HTTP POST 可注入）；`OAuthTokenStore`（按 provider 分文件，权限 0600，`AGENT_OAUTH_TOKEN_DIR` 可重定向，过期判断）。
- Vertex：Google OAuth2 授权码流转（ADC 之外新增）；Bedrock：AWS IAM Identity Center OIDC 授权码流转。
- `resolve_vertex/bedrock_credentials` 现优先用持久化 OAuth 令牌（未过期直接可用；过期有 refresh_token 则尝试刷新，失败回退 ADC/IAM），优雅降级不变。
- 测试：`test_oauth_flow.py`(9) 全绿（PKCE/URL/交换/刷新/store/complete/resolve 优先+刷新）；旧 `test_oauth_credentials.py`(5) 仍绿。

## ⑤ CI 全量门禁
- 现状：CI 的 `test` job 只跑 `npm test`（JS），**完全没有 Python pytest 门禁**。
- `pyproject.toml` 已声明 `[project.optional-dependencies].test = [pytest, pytest-asyncio, httpx, ...]` 且 `asyncio_mode="auto"`、`testpaths=["tests"]`。
- 改动 `.github/workflows/backend-ci-cd.yml`：新增 `python-test` job（setup-python 3.13 → `pip install -e ".[test]"` → `python -m pytest -q` 全量门禁）；`deploy-staging`/`deploy-production` 的 needs 增加 `python-test`。lint job 仍保留恒真护栏 + doc-derived-audit。
- 本地验证：在托管 venv 装 `pytest pytest-asyncio fastapi httpx starlette` 后，`test_mcp_tool_bridge.py`(async) 与 `test_admin_api.py`(TestClient) 现可正常收集/运行（旧"unknown pytest.mark.asyncio"缺口消除）；引擎相关集成测试因缺 `litellm`（主依赖，CI 的 `.[test]` 会装）暂待 litellm 安装后跑全量验证。

---

## ⑥ P0/P1 差距收口（审计任务推进，下午续做）

### 上下文
前半段已建：审计 `docs/Agent_Technical_Maturity_Audit_2026-07-18.md`；三差距基础设施 `distributed_lock.py`/`slo_collector.py`/`api/slo.py` + 三测试文件 + `verify_production_readiness.py` + `PRODUCTION_READINESS_RUNBOOK.md`；`message_queue.py` 补 Redis 消费者循环；`cron.py` 接锁+MQ。本段完成"接线 + 测试通过 + 收尾"。

### 接线（main.py）
- `main.py` 新增 `from agent.api.slo import router as slo_router` + `from agent.infrastructure.slo_collector import get_slo_collector`。
- `app.include_router(slo_router, prefix="/v1")` → `/v1/health/slo` 上线。
- `MetricsMiddleware.send_wrapper` 在每次 HTTP 响应后 `get_slo_collector().record(duration_ms, is_error=status>=400)`，使 SLO 有真实数据源。
- `infrastructure/__init__.py` 导出 `DistributedLock/RedisLock/LocalLock/LockManager/create_lock/get_lock_manager`。

### 配置（deploy/kubernetes/configmap.yaml）
- 补 `MQ_ENABLED: 'true'` + `MQ_STREAM_PREFIX: 'jiabaixing'`（激活 MQ 主干）。
- 修端口错位：补 `AGENT_PORT: '8765'`（app 默认 3112，但 K8s Service/containerPort/PYTHON_AGENT_URL 均为 8765；docker-compose 已设 8765。漏配会导致 K8s 内 Python 不可达，连带锁/MQ/SLO 全失效）。

### 测试修复（关键）
- SLO 测试原用单例 `get_slo_collector()`，跨用例互相污染 → 改为每个用例 `SLOCollector()` 新实例（4 用例独立）。
- `test_lock_manager_caches_instance`：原 `_uniq_name("mgr")` 每次生成不同名 → 改为同一 `name` 两次 `get` 验证缓存。
- 死信阈值：`message_queue.py` 三处 `if msg.retry_count >= msg.max_retries` 保持（**回退误改的 `>`**）——项目既有 `test_message_queue.py::test_dead_letter_after_max_retries` 以 `>=` 为契约（max_retries=2 → 2 次投递后死信）。新 `test_fallback_dead_letter_on_persistent_failure` 改为断言 `len(attempts)==2`。
- Redis re-XADD bug 修复保留：`"max_retries": str(msg.retry_count)` → `str(msg.max_retries)`，并新增 `"retry_count": str(msg.retry_count)` 字段让重投可累计重试次数（否则 Redis 死信永不触发）。
- `verify_production_readiness.py` 修 typo `CHECKKS`→`CHECKS`。

### 验证结果
- 新测试 13 passed / 4 skipped(Redis 门控)；恒真护栏 ✅ 无恒真；`import agent.main, ...` OK。
- 回归：现有 `test_message_queue.py` 15 passed(1 旧死信用例仍过)；`test_phase5.py`+`test_business_logic_audit_fixes.py` 64 passed（cron 调度路径未回归）。
- `verify_production_readiness.py` 在 REDIS_ENABLED=false 下正确报红/黄（部署态核查可用）。

### 结论
3 个 P0/P1 差距在 Python 端全部落地 + TS 路由联通 + 测试全绿 + 端到端 SLO 可核查；K8s 端口错位修复。残留：水平扩展/分片、jobs.json 并发、multi_agent 仍进程内队列。

---

## ⑦ 生产态最后一公里（真实流量验证，晚间续做）

### 主线（用户指令）
"把研发态硬核（锁、MQ 主干、SLO）推到生产态，并用真实流量完成最后一公里验证"——不做补能力，做上线前实证。

### 真实流量探针 `python/scripts/live_smoke.py`（新增）
- 启动**真实** `agent.main:app`（同一套 MetricsMiddleware + `/v1/health/slo`），仅把 lifespan 替换为 noop（绕过 `engine.initialize()`/litellm，纯 HTTP 层验证不需要）。
- 启动坑：venv python 是 Windows 原生，`PYTHONPATH` 必须用 Windows 路径 `C:/zy/jiabaixing/python`，Git Bash 的 `/c/...` 形式不被识别（报 `No module named 'agent'`）。

### 发现的 2 个 SLO 生产态真实 bug（已修复）
1. **`MetricsMiddleware` 形参错配**：调用 `record(duration_ms=...)`，收集器形参实为 `latency_ms` → `TypeError` 被旧 `except:pass` 静默吞掉，SLO 长期记 0（`/v1/health/slo` 永远 `total_requests=0`，但 `/v1/metrics` 的 `_request_metrics` 正常 → 定位到是 record 抛错被吞）。改为 `record(latency_ms=...)`，异常改 `log.warning` 显式可见。
2. **中间件顺序导致 429 绕过 SLO**：原 `ApiGatewayMiddleware`(限流) 为最外层，其直发 `429` 绕过 `MetricsMiddleware` → 限流/鉴权错误不进 SLO。重排为 `CORS(最外)→Metrics→ApiGateway(最内)`，现 429 也被记录（`is_error=True`）。

### 验证脚本 `verify_production_readiness.py` 修复
- `_redis_ping` 原无 `socket_connect_timeout`，本机无 Redis 时卡 OS TCP 重传超时（分钟级）→ 加 `socket_connect_timeout=2/socket_timeout=2` + `asyncio.wait_for(...,2)` 快速失败。
- `_slo_base` 原默认 `localhost` → 部分平台解析 IPv6 `::1`，uvicorn 仅 IPv4 `0.0.0.0`，每次请求卡 ~2s 且打不到 SLO 中间件 → 默认改 `127.0.0.1` 并归一化 `localhost`。
- 新增 `_check_real_traffic(n)` + `--traffic N` 开关：发 N 次真实 HTTP 请求后断言 SLO `window.total_requests ≥ 50%`。

### 最后一公里结果（GREEN）
- `live_smoke.py` 起服务 → `verify ... --traffic 100`（附 `OTEL_ENABLED=true AGENT_REPLICAS=2 SHARD_COUNT=2`）：
  `[绿] 真实流量验证: 真实流量 100 次(成功 100), SLO 已记录样本=133, 成功率=1.0, P95=2.4ms, 状态=ok`。
- `[绿] 副本数(AGENT_REPLICAS=2)`、`[绿] 可观测性(OTel+SLO)`、`[红] 分布式锁/消息队列主干`（本机无 Redis → 红，属部署前置；K8s configmap 已 `REDIS_ENABLED=true`/`MQ_ENABLED=true` + 同 namespace Redis 可达即绿）。

### 水平扩展收口（残留项，本次确认完成）
- `infrastructure/sharding.py`：`consistent_shard`/`get_shard_count`/`get_replica_index`/`this_replica_owns`/`LeaderElection`(基于分布式锁)。
- `scheduler/cron.py`：`LeaderElection("cron")`，仅 leader 跑 `_tick_loop`(失锁即停)；全副本订阅 MQ 执行 job；`_save()` 加线程写锁。
- `evolution/multi_agent.py`：切到 `create_message_queue()`(本地即时投递 + 跨副本 Redis Streams + 跳自身回声)。
- 测试 `test_sharding.py`/`test_multi_agent_mq.py`/`test_cron_leader.py` 全绿。

### 回归与护栏
- 本次新增/相关测试 39 passed / 2 skipped(Redis 门控)；`test_slo_endpoint.py` 5 passed；恒真护栏 ✅。
- **`tests/test_gateway.py` 12 例失败已修复（非"预存无关"）**：经排查是生产路径真实缺陷——`MessageDispatcher` 在 `engine.py:3214` 引擎初始化时实例化，其 `__init__` 调 `set_send_function(self._mirror_send)` 但类内无该方法 → `AttributeError` 致引擎初始化即崩。已补 `_mirror_send(target_chat, content)`(按 `platform#chat` 路由到同名注册适配器)。见 §⑧。

### 文档
- `docs/PRODUCTION_READINESS_RUNBOOK.md`：§0 表加水平扩展行；§3.2 补 SLO 两 bug 修复说明；§3.5 新增"最后一公里真实流量验证"(`--traffic N` + Windows IPv4 注意 + 本机无 Redis 红项说明)；§5 更新残留(multi_agent 已切 MQ / jobs.json 已缓解)；§6 新增水平扩展章。
- K8s configmap 已含 `AGENT_REPLICAS='2'` + `SHARD_COUNT='2'`(本会话补) + `REDIS_ENABLED/MQ_ENABLED/AGENT_PORT`(前会话补)。

### 当前生产硬核完成度（审计结论）
- 锁/MQ/SLO：代码完成 + K8s 配置激活 + 单测覆盖(Redis 门控)；SLO 经真实流量验证为绿。
- 水平扩展：分片 + 选主 + cron/multi_agent 接入完成 + 单测绿。
- 唯一未端到端验证项 = 锁/MQ 的 Redis 真连通，因本开发机无 Redis/Docker；属部署环境缺口，非代码缺陷。

---

## ⑧ 网关包生产态缺陷深挖（用户指令"这步做不了，你继续做别的"）

### 上下文
锁/MQ 的本地 Redis 验证受限于沙箱（无 Redis/Docker，且不用 fakeredis 冒充真实跨实例证据）。用户让我转向其他生产态工作。选了"消除测试红 + 排查隐藏缺陷"主线。

### 修复的 3 个真实生产路径缺陷
1. **`agent/gateway/dispatcher.py` 缺 `_mirror_send`**（实例化期 `AttributeError`）：`__init__` 调 `self._mirror.set_send_function(self._mirror_send)`，类内无此方法 → `MessageDispatcher()` 一实例化就崩。**生产影响**：`engine.py:3214-3215` 在 `AgentCore` 初始化时 `MessageDispatcher()`，引擎启动即崩。补 `async _mirror_send(self, target_chat, content)`：解析 `platform#chat`，按 platform 名在 `_adapters` 找适配器（退化匹配名称包含者），调用 `adapter.send_message(chat_id, content)`，无匹配/异常返回 `False` 并记日志（不抛）。
2. **`agent/gateway/platform_manager.py:114` `IndentationError`**：被注入垃圾字符 `xi`（行首 `xi            await adapter.send_message(...)`）→ `try:` 后无合法缩进块。删 `xi` 即可。**生产影响**：`engine._init_platform_manager`(`critical=False`) lazy 构建 `PlatformManager()` 时静默失败，平台管理子系统失效但不阻断启动。
3. **`agent/gateway/platforms/relay_adapter.py` `NameError: dataclass`**：用 `@dataclass` 却缺 `from dataclasses import dataclass`。补 import。**生产影响**：`engine._init_relay_adapter`(`critical=False`) lazy 构建 `RelayAdapter()` 时静默失败，中继子系统失效。

### 验证
- `tests/test_gateway.py`：**33/33 通过**（原 12 失败全消）。
- 全 `agent` 包导入扫描：**282/282 模块导入通过，0 代码缺陷、0 环境失败**（pkgutil.walk_packages + importlib）。
- 直接实例化 `PlatformManager()` / `RelayAdapter()`：均 OK。
- 注意：这 3 个缺陷此前无任何专门测试覆盖（grep 全仓无 `platform_manager`/`relay_adapter` 测试），且 `critical=False` 使其在生产中不抛启动错误，故一直被静默放过——属"生产态盲区"。

### 代码/测试改动文件
- `python/agent/gateway/dispatcher.py`（+`_mirror_send` 方法）
- `python/agent/gateway/platform_manager.py`（删行首 `xi`）
- `python/agent/gateway/platforms/relay_adapter.py`（+`from dataclasses import dataclass`）
- MEMORY.md 第 39 行 + 本日志 §⑦ line105 同步校正。

### 下一步可选（未做）
- 为 `PlatformManager`/`RelayAdapter` 补单元测试（当前盲区）。
- 把"全包导入扫描"做成 CI 红线（捕获导入/实例化期硬伤，避免 `critical=False` 静默吞缺陷）。
- 锁/MQ 的 Redis 真连通验证需真实 Redis（沙箱不可得）。

---

## ⑨ 填单测盲区 + 导入扫描 CI 红线（用户选 ①+② 都做）

### 交付 1：PlatformManager / RelayAdapter 单测（填盲区）
- `tests/test_gateway.py` 新增 `TestPlatformManager`(11 例) + `TestRelayAdapter`(11 例) 共 **26 例**，复用文件内 `MockAdapter`。
- 写测试过程**又揪出 2 个真实代码缺陷**（此前零覆盖所以没暴露）：
  1. **`platform_manager.py` `get_status`/`get_all_statuses` 调 `is_connected()` 未 `await`**：`is_connected` 在 `base.py` ABC 声明为 `async`，但 manager 同步调用，致 `status.connected` 被赋为协程而非布尔 → 连接状态上报失真。改为 `async` + `await`。（已确认 `PlatformManager.get_status/get_all_statuses` 在 `agent/` 无真实调用方，仅 `agent_factory`/`voice_mode` 的同名方法是别的对象，故改签名为安全。）
  2. **`relay_adapter.py` `send_message` 模拟模式计数缺失**：`_ws is None` 时返回 `True` 却不 `self._stats["sent"] += 1` → 模拟发送不被计入统计。已修正。
- 结果：`tests/test_gateway.py` **59/59 通过**（原 33 + 新 26）。

### 交付 2：全包导入扫描 CI 红线
- 新建 `python/scripts/check_import_scan.py`：① 遍历全部 `agent.*` 导入，抓 `SyntaxError`/`IndentationError`/`NameError`/`AttributeError`/本地包缺失 `ImportError`（第三方 `No module` 仅告警不阻断）；② 对引擎启动必构造子系统 `MessageDispatcher`/`PlatformManager`/`RelayAdapter` 做**无参实例化**，抓实例化期 `AttributeError`（纯导入扫不到，正是 §⑧ bug#1 那类）。发现代码缺陷即 `exit 1`。脚本自带 `sys.path` 自引导（从自身位置推 `python/` 根）。
- 接入 `.github/workflows/backend-ci-cd.yml` 的 `python-test` job 末尾：`python scripts/check_import_scan.py`（job workdir 已是 `python`）。
- 本地验证：`[OK] 导入通过模块: 282/282` / `[PASS] 全包导入扫描 + 关键子系统实例化均无代码缺陷` / exit 0。恒真护栏 ✅。

### 全量回归说明
- 全量 `pytest` 仍 **48 失败、2624 通过**，但 48 例全部位于无关模块（test_database/loop/multimodal/otel/p0_migration/verification_constraints/credential_cost/shell/sandbox/phase7/phase_b_e2e/result_aggregator/new_modules 等），grep 确认**无一 import 网关模块**，属预存失败，非本次引入。本次所改 `gateway/*` + `test_gateway.py` 全部绿。

### ⚠️ 关键前置（务必告知用户）
- `git status` 显示 `python/agent/gateway/`（整个包）+ `python/tests/test_gateway.py` + `python/scripts/check_import_scan.py` 当前均为 **untracked（未纳入版本控制）**。CI 步骤 `python scripts/check_import_scan.py` 在仓库中能扫到 `agent.gateway` 的前提是这些文件被 `git add`/提交。否则 CI 会因找不到关键子系统模块而阻断。需用户提交（其本地 Git 提交是既定任务之一）。

### 改动文件
- `python/tests/test_gateway.py`（+26 网关单测）
- `python/agent/gateway/platform_manager.py`（`get_status`/`get_all_statuses` → async+await）
- `python/agent/gateway/platforms/relay_adapter.py`（删冗余底部 `from dataclasses import dataclass`；`send_message` 模拟模式计数修正）
- `python/scripts/check_import_scan.py`（新增红线脚本）
- `.github/workflows/backend-ci-cd.yml`（python-test job 接入扫描）
- `docs/PRODUCTION_READINESS_RUNBOOK.md`（§7 新增导入扫描红线章）

---

## ⑩ 收尾：清理其余预存失败模块并整批提交（含网关包）

### 上下文（用户指令）
"② 继续清理其他预存失败模块； 修完错误再提交。提交这批改动（含网关包）"——把 §⑦/§⑧/§⑨ 之外的其余预存失败测试对齐当前 API、修完、整批提交。锁/MQ 的本地 Redis 验证（沙箱无 Redis/Docker，且禁用 fakeredis）此前已确认做不了，本批不含。

### 其余预存失败模块的修复
1. **`tests/integration/test_module_collaboration.py`（4 处陈旧 API）**：`test_parallel_task_grouping` 补 `CausalGraphNode` import；`test_quality_scoring` 由 `LoopEvaluator()` 改为 `Evaluator(llm=MagicMock())`；`test_sensitive_content_detection` 由 `SensitiveDetector()` 改为 `check_sensitive_info(..., scene=CheckScene.OUTPUT)` 并断言 `SensitiveCheckResult`；`test_batch_processor_creation` 由 `BatchProcessor(batch_size=10)` 改为 `BatchProcessor(BatchConfig(concurrency=10))`。→ 14 passed。
2. **`tests/integration/test_redis_memory_pipeline.py`（9 处陈旧 API）**：`Evaluator(LoopContext/StepResult)` 对齐；`OutputGuardrailEngine().check()` 返回 `GuardrailResult`（密码用例加引号）；`A2AAgentCard/A2ACapability/A2ATask/A2ATaskStatus` 新形状；`MCPToolBridge(provider=...).sync_to_registry(registry)` + 工具名 `mcp_{server}_{tool}`；`CausalGraph` 断言；otel 改用 `init_tracer/get_tracer` + `init_metrics/get_meter/loop_iterations_counter`（`OTEL_ENABLED=false`）。→ 16 passed。
3. **`tests/test_phase_b_e2e.py`（3 失败，仅全量时复现）**：根因 = 全文件共用同一 `client_id` 的全局令牌桶被 29 个测试耗尽 → 后续 GET 撞 429。`client` fixture 注入独立 `x-forwarded-for: f"test-{uuid.uuid4().hex}"`；`test_full_pipeline_all_entries` 每 entry 独立 `x-forwarded-for: 10.0.0.{idx+1}` 并把接受态改为 `(200,502,503)`（不再依赖 429）。→ 29 passed。
4. **`tests/test_otel.py`（15 passed）**：沙箱缺 `opentelemetry-exporter-*` → `pip install opentelemetry-exporter-otlp-proto-grpc opentelemetry-exporter-prometheus opentelemetry-instrumentation-fastapi` 补齐声明依赖（CI 的 `.[test]` 本就包含），本地与 CI 对齐。

### 全量回归 + CI 护栏
- 全量 `pytest`：**2672 passed / 11 skipped / 0 failed**（48 例历史失败已被本次清理清零，无新增失败）。
- 导入扫描红线 `scripts/check_import_scan.py`：**282/282 通过**，exit 0。
- 恒真护栏 `scripts/check-no-tautology-tests.mjs`：✅ 无恒真断言。

### 暂存与提交
- 暂存 355 文件：`python/agent/gateway/*` + `python/tests/*` + 生产硬核(锁/MQ/SLO/分片) + 测试清理 + 枢纽迁移 bridge 重命名 + 配置/文档/脚本；排除 `.workbuddy`/`.codebuddy`/`dump.rdb`/截图/lint 日志/审计 HTML/MD/`src/frontend/*`/`src/gateway/*`（通过 `git add python/ ':!python/.coverage' ':!python/nul' ':!python/jiabaixing_agent.egg-info'` + 显式路径列表），`grep -E` 排除校验 = CLEAN。
- 提交 `52e70ab`（`feat(gateway): 接入 Python 网关包并完成生产态硬核与预存失败测试清理`），355 files changed (+83,736/−2,353)。
- 剩余 380 uncommitted = 故意排除的垃圾/独立特性文件（非本次范围）。

### 结论
用户指令"清理其余预存失败 + 修完提交 + 含网关包"已完整完成：所有预存失败测试已对齐当前 API 并清零，网关包随 355 文件整批提交，全量 0 失败 + 导入扫描/恒真护栏全绿。下一步可选：真实 Redis 连通验证(待环境)、jobs.json 迁 Redis/SQLite 根治竞态。
