# 2026-08-13 工作日志

## F1 残留修复闭环 + P1/P2 真值复核 (续 2026-08-12 F1 Phase1)

### 一、P0 残留 3 项已闭环并验证
文件: `src/harness/tools/network/web_fetch.ts` + `src/harness/tools/file/file_read.ts`
1. **SSRF 混合编码绕过** → 新增 `parseMixedBaseIpv4`(首段 hex + 余 decimal, 如 `0x7f.0.0.1`/`0177.0.0.1`), 在 `isSafeUrl` 的 `return true` 前先过混合进制归一化 + `isSafeIPv4` 校验。
2. **httpClient 无超时 + 全缓冲 DoS** → 新增 `withTimeout()` 竞速封装(25s, `WEB_FETCH_TIMEOUT_MS` env 可配) + `WEB_FETCH_MAX_BUFFER_CHARS`(16MB 截断); 注入路径与自管 fetch 路径**均覆盖**。
3. **symlink 逃逸** → `resolveWithinRoot` 真实存在时 `realpathSync` 复检, 越界抛"symlink 逃逸"; catch 仅兜底 `ENOENT`, 其余错误上抛(修了我自己引入的 catch 吞 rejection bug)。
- 运行时验证 `verify.cjs` 20/20 PASS; jest 镜像 `tests/unit/tools/security_residuals.test.ts` 已建; 临时目录已清理。
- ⚠️ 变更**未提交**(按用户惯例, 未经显式要求不 commit)。

### 二、P1/P2 整层实时复核 (Explore 子代理逐文件读, 当前真值)
- **REFUTED(已修, 无需再动)**: B3 web_fetch 注入路径三护栏(isSafeUrl+withTimeout+16MB截断); C3 image_generate ToolRegistry 会话配额+去重已加。
- **PARTIAL**: D1 code_fix FILE_WRITE 越权已改 CODE_EXECUTE+标注"轻量规则"(仍仅 preview, 不写盘); D5 code 三件套已有 executor 单测但 LLM 输出未覆盖; F1 emotion_detect 合规但 self_reflect 缺 try/catch; F2 self_reflect 假成功已修但仍缺 try/catch + requiredParams 未校验。
- **CONFIRMED 仍 OPEN(19 项)**: 见 `capability_audit_p1p2_remediation.html`。
- 高优先级 OPEN: E3(task/note/calendar 并发 RMW 无锁丢更新)、C1(tts_speak 假成功)、A1(memory_store 缺依赖静默成功)、E1(calendar 缺 deps 守卫)、E6(calendar 零测试)、B1(web_search TS 本地实现违反 AGENTS.md §0.1, 应与 F1 同法归 Python canonical)。

### 三、下一步
产出 remediation 路线图(Phase B 基础可靠性 → Phase A 架构对齐), 待用户确认执行范围。详见 HTML 报告。

### 四、Phase B-1/B-2/B-4 + B1 落地(已执行并验证, 用户确认范围)
用户确认: "先执行 Phase B(B-1/B-2/B-4 零行为歧义、最高杠杆), 再进 Phase A 的 B1 架构归一"。

- **B-1 并发 RMW 原子锁**: 新增 `src/harness/tools/daily/storeLock.ts`(`withStoreLock` — WeakMap 键 Promise 链串行化每个 store 对象的 read-modify-write, 错误不破坏链)。接入 `task_manage.ts`(complete/update/create/delete 4 分支)、`note_take.ts`(write 分支)、`calendar.ts`(create_event/delete_event/set_reminder 3 分支)。
- **B-2 memory_store 诚实失败**: `memory/memory_store.ts` 此前两依赖均缺时返回 `success:true "已存储"` 静默成功 → 改为返回 `success:false "存储失败: 未注入记忆存储后端"`。
- **B-4 calendar deps 守卫**: `calendar.ts` 在 `action` 解析后若 `!deps.calendarStore` 返回诚实 `success:false`(零行为歧义); 与 B-1 锁同文件。
- **B1 web_search 架构归一(AGENTS.md §0.1)**: `network/web_search.ts` 新增 Python canonical 代理 — 优先 `deps.searchEngine` → `getActivePythonBridge().toolsetExecuteRaw('web_search', ...)`(F1 同法, 逻辑失败降级 TS 本地, transport 失败亦降级, 绝不自管逻辑拒绝) → TS 本地多引擎 fallback。确认 Python `python/agent/tools/network_tools.py:web_search_executor` 经 `/api/tools/execute` 可达。
- **验证**: 6 文件 `tsc` 语法全 OK; `.b1_verify/verify.cjs` 14/14 运行时 PASS; 新建 3 个 jest CI 镜像(`tests/unit/tools/daily/store_concurrency.test.ts`、`tests/unit/tools/memory/memory_store.b1.test.ts`、`tests/unit/tools/network/web_search.b1.test.ts`)。临时 `.b1_verify/` 已清理。
- ⚠️ 以上变更**未提交**(按用户惯例, 未经显式要求不 commit)。
- **剩余 Phase A 项待办**(B1 已做, 余): A-2 code_review(D2/D3/D5 一致性)、A-4 image_generate 状态分级+429 退避、A-5 tts_speak 参数透传、A-6 calendar 时区/提醒、A-7 memory 晋升。待用户确认是否继续。

### 五、高优先 P1 表落地(用户直接给 P1 表, 实时复核+修复)
用户给 P1 表: E3/C1/A1/E1/B1/E6-A5/D2/F2。先逐行实时复核(不采信旧结论):
- **已闭环(本轮前 Phase B 完成, 本次复核确认)**: E3(withStoreLock 在 task/note/calendar 三文件)、A1(memory_store.ts:201 诚实失败)、E1(calendar.ts:188 deps 守卫)、B1(web_search.ts:425 Python 代理)。四项经 grep 行号确认真实存在。
- **本轮新修复 4 项**:
  - **C1 tts_speak 假成功**: `network/tts_speak.ts` 此前无合成器/合成器抛错均返回 `success:true "[模拟模式]"` → 改为诚实 `success:false`(未配置/调用失败均失败, 仅真实 `result.success` 才成功)。
  - **F2 self_reflect 持久化无 try/catch**: `cognition/self_reflect.ts` 新增 Logger 导入, 将 `recordExecution` + `reflectionStore.add` 包入 try/catch, 抛错返回诚实 `success:false`(不再让 executor 崩溃)。
  - **D2 code_review 描述夸大+LLM 静默吞**: `code/code_review.ts` ① 修正 description 与文件头注释, 去掉未实现的"语法/性能"维度, 如实描述"规则检查+安全扫描+可选LLM"; ② `catch {}` 空吞改为 `Logger.warn` + `llmFailed` 标记, 报告与 metadata 如实呈现(不再静默)。
  - **E6/A5 memory_search 假成功+零测试**: `memory/memory_search.ts` 无后端/异常均改为 `success:false` 诚实失败(与 A1 同类的假成功, 顺带修掉); 另新建 `tests/unit/tools/memory/memory_search.b1.test.ts`。
- **E6/A5 测试覆盖现状**: calendar→`store_concurrency.test.ts`、memory_store→`memory_store.b1.test.ts`、memory_search→`memory_search.b1.test.ts` 三处均有 executor 单测; 零测试项已清。
- **验证**: `.p1b_verify/verify.cjs`(tsc.transpileModule + 自定义 require 桩, ts-runtime-verify 法) **21/21 运行时 PASS**(C1×5 / F2×5 / D2×7 / E6-A5×4); 另建 4 个 jest CI 镜像(`tts_speak.b1`/`self_reflect.b1`/`code_review.b1`/`memory_search.b1`)。临时目录已清理。
- ⚠️ 以上变更**未提交**(按用户惯例, 未经显式要求不 commit)。
- **P1 表全清**。剩余为更低优先 Phase A 项(A-2 余 D3/D5、A-4/A-5/A-6/A-7), 待用户确认是否继续

### 六、核心代码审计 — 残留 4 项 TS 缺陷已闭环(本轮)
- 报告: `jiabaixing_core_audit_2026-08-13.html` 第六节(原"残留问题"已改写为"残留问题修复结果(本轮已全数处理)")。
- 审计累计 20 处缺陷(Python 8 + TS 12)。本轮收口报告原列 4 项残留 TS 建议项:
  1. `verification/VerificationService.ts:212` 错误指示词误判 → 新增 `stripCodeSpans()`(剥离 ` ```...``` ` 与行内 ` `...` `)仅对自然语言判定; 英文 error/failed 改词边界正则(`\berror\b`/`\bfailed\b`), 避免 terror/errorless 误命中, 消除教程/代码内容触发的无谓 replan。
  2. `orchestration/ScenarioAwareScheduler.ts:347,349,1243`(实际路径 `src/core/`)fire-and-forget → `void fn()` 改 `.catch(Logger.warn)`, 消除未处理 Promise 拒绝。
  3. `AgentHarness.ts:607` 进化反馈未 await → AFTER_RESPONSE 钩子内 collectFeedback/assessQuality/generateSkill 均补 `await` + 整体 `try/catch`(失败仅 Logger.warn, 不影响主响应)。
  4. `orchestration/ResultAggregator.ts:287` winnerTaskId 未校验 → 增加 undefined/involvedTasks 成员校验, 无效回退 `involvedTasks[0]`(与 catch 分支一致), 消除 `"undefined: ..."` 下游误过滤。
- 验证: TS transpile API 对 11 个本轮累计修改的 TS 文件(4 残留 + 7 前次)语法校验全部 OK。⚠️ jest 全量回归待 `npm ci` 修复依赖后执行(本机 node_modules 损坏, 无运行时验证)。
- ⚠️ 变更未提交(用户惯例)。。
