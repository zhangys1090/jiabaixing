"use strict";
/**
 * 统一进化引擎接口
 *
 * 【架构定位】
 * 所有进化引擎（V1、V2、未来的V3等）都应实现此接口
 * 便于统一调度、监控和管理
 *
 * 设计原则：
 * - 统一的方法签名
 * - 统一的类型定义
 * - 统一的生命周期管理
 * - 向后兼容，不破坏现有实现
 *
 * 实施阶段：
 * - 第一阶段（当前）：接口定义 + 适配器模式
 * - 第二阶段：各引擎逐步适配
 * - 第三阶段：统一调度器使用统一接口
 */
Object.defineProperty(exports, "__esModule", { value: true });
